// Template: three failures found in play: one agent each to the root cause, one fixer, one reviewer (the Pokedex bugs, 2026-09-24).
// Kept from .rounds/r7-land/dex-bugs-wf_60c15332-6ac.js; tools/newgold/rounds/README.md says how a round runs.
export const meta = {
  name: 'dex-bugs',
  description: "Three Pokedex failures found while paging entries: Enamorus's page asserts, DETAILS goes black, a search result fails an allocation — diagnose each, fix, review",
  phases: [
    { title: 'Diagnose', detail: 'one agent per failure, with the harness, to the root cause' },
    { title: 'Fix', detail: 'every confirmed cause, decomp first where needed, tests, builds, screenshots' },
    { title: 'Review', detail: 'the fixes hold, nothing else in the Dex regressed' },
  ],
}

// Where things were on the machine these ran on; set them for yours.
const HOME_ = '/home/paolo'
const TREE = `${HOME_}/hgss-newgold`                     // the main tree, branch newgold
const WORKTREES = `${HOME_}/hgss-worktrees`              // one git worktree per agent
const ROUNDS = `${WORKTREES}/.rounds`                    // rows, reports, scratch: on disk, never /tmp
const MEMORY = `${HOME_}/.claude/projects/-home-paolo-Porting-HGSS/memory`
const ENV = `${HOME_}/hgss-build/env.sh`                 // the project's own wine
const SAVES = `${HOME_}/hgss-saves`                      // Paolo's saves: scratch copies only
const REF = `${HOME_}/Porting HGSS/hg-engine-newgold-reference`
const CAPPED = `${TREE}/tools/newgold/devkit/capped`     // every heavy command runs under it
const MERGE_TOOLS = `${TREE}/tools/newgold/rounds`
const WT = `${WORKTREES}/saveui`
const SCR = `${ROUNDS}/dex-bugs`
const CONTEXT = `
PROJECT: ${TREE} is pokeheartgold (HeartGold decompilation) with hg-engine (engine layer, reference d0380a487) and konefr's New Gold (reference 1fa3c9366) ported into it as a real decompilation: 1025 national species plus forms (species 1042..1437) where HeartGold had 493. Reference repository ${REF}, read only through git at those two revisions. Port rules: assembly that needs a change is first decompiled to MATCHING C in its own commit (HeartGold and SoulSilver byte-identical before/after, md5s in the message), then the behaviour change separately; reference defects fixed canonically with the reason in the commit; one commit, one thing. Read ${MEMORY}/hgss-harness-core-raises-no-data-abort.md, hgss-diagnostics-are-a-compile-time-folder.md, hgss-build-needs-project-wine.md and hgss-scratch-on-disk.md first.

THE FAILURES (found by the dex-pages work on newgold at e36dd7b0c, still there at cc2c01c5b, the current newgold tip; its harness scripts and notes are in ${ROUNDS}/dex-pages/ -- harness/dexshot.py, period.py, capture.py, r7-ui's core.py and species.py; review/dexrun.py; screenshots shots/enamorus_*_assert.png):
A. Enamorus's Dex page hits an assertion in the Pokemon picture loader and then shows the communication error screen.
B. DETAILS on Iron Crown, Pecharunt or Lokix leaves both screens black (the Dex heap's least-left marker reads 0x24).
C. Moving onto any search result fails to allocate 34,516 bytes from the Dex heap (so the results page, window 85 of ov18_021F9FDC, never shows).
What is already known of each (from the dex-pages builder's notes): A -- Enamorus is species 926, national 905; the assert is in AllocAndReadFromNarcMemberByPathAndId, called from AllocAndReadWholeNarcMemberByIdPair < sub_0201401C < sub_02014128 < sub_02014178 < sub_02014494 (src/unk_02013FDC.c, the Pokemon picture loader); Iron Crown, Pecharunt and Lokix load fine; Enamorus has a Therian form (suspects: the Dex's seen-form data, the form-to-sprite mapping, or savedit.set_dex not setting something the game would). B -- DETAILS (A on the Dex's front page) for Iron Crown, Pecharunt or Lokix turns both screens black for good with no assert and no failed allocation; for Bulbasaur or Arceus it opens the AREA page (first suspect: the area page's data for species past 493). C -- SEARCH (Y), START (touch 128,176), then RIGHT onto a result (only IRON_BOULDER and IRON_CROWN seen): an allocation of 34516 bytes from HEAP_ID_POKEDEX_APP (heap 37) fails, then the communication error (the Dex heap is made in Pokedex_Init with 0x67000: is it or the allocation wrong?). The harness recipe: ${ROUNDS}/dex-pages/harness/dexshot.py -- 'ONLY=1 python3 dexshot.py TREE OUT SPECIES "X:30,A:200,A:200,shot,..."' makes a save where only SPECIES is seen and caught, opens the Dex from Violet's Pokemon Center and runs the steps ('T:x:y:wait' touches the bottom screen, 'B:200' etc. press buttons), printing markers.describe at the end; it needs r7-ui's core.py beside it. B and C may share a cause (a Dex heap sized for 493 species and now asked for 1025-species tables, or a leak) -- find out. The reference's hg-engine may have resized or reworked these (read its code at d0380a487 for the Dex app's heap and pictures), and HeartGold retail is the baseline for what the heap held.

RESUMING: an earlier attempt of this same job was killed mid-way when the PC ran out of RAM. Look first at what it left -- its scratch folder, 'git status', 'git log', a rebase or cherry-pick in progress ('git status' says so; finish or abort it deliberately) -- keep what is sound and continue; do not redo finished work.

MEMORY CAP (the PC ran out of RAM twice: one harness walk script, run in three parallel copies, grew to 12 GB each in 80 seconds): run EVERY emulator, harness, species walk, gym replay and test-suite command through ${CAPPED} (a kernel memory cap in its own scope: 'capped python3 walk.py ...', default 4G; 'capped -m 8G make -j12 ...' for builds); a process that outgrows its cap is killed alone. Watch a long run's memory ('ps -o rss' or free -g) in its first minute and stop it if it keeps growing; in harness loops do not unpack the whole 4 MB RAM into Python ints every frame.

RESOURCE HYGIENE: other agents run at the same time -- at most three emulator/harness processes of your own at once, species.py walks with at most 4 jobs, builds with make -j12, check 'free -g' before a long run and wait if under 4 GB is available; delete ROM copies you no longer need from ${SCR}; stop every process you started before you finish.

RULES: the diagnostics ROM built at the dex-pages tip (same code as cc2c01c5b) is ${WT}/build/heartgold.us.diag/pokeheartgold.us.nds -- copy it (and anything you write) under ${SCR}/<your role>/; the Diagnose stage never builds in ${WT} and commits nothing. Scratch, logs, dumps, ROM copies go under ${SCR}/ (never /tmp, it is a RAM disk; run tests with TMPDIR=${SCR}/tmp -- test_saveui's fake-ROM test needs the real /tmp, ignore that one). Build ALWAYS as '. ${ENV} && make -j12 COMPARE=0 <target>' in ${WT} (ROM targets directly; GAME_VERSION=SOULSILVER; NEWGOLD_DIAG=1). Never touch ${TREE}'s working tree, never push. Never 'pkill -f' a pattern in your own command line. Commits: short lowercase subject 'area: ...', plain prose body (what and why, numbers measured), last line 'Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>'.`

const DIAG = [
  { key: 'enamorus', prompt: `YOUR FAILURE: A (Enamorus). Reproduce it; find which assertion (the diagnostics' assert markers name the file/line), which archive member the picture loader asked for (species, form, gender, shiny, front/back), why it is wrong or missing (the pokegra archive and the sprite importer tools/newgold/import/import_sprites.py, the species -> picture index mapping in src/pokemon.c, Enamorus's forms 'Incarnate'/'Therian' and its gender ratio -- it is female-only), and whether any other species or form hits the same path (walk every species' Dex page with species.py or your own loop over the national numbers and report every one that asserts). The communication error screen is what the game shows after the assert: find the chain.` },
  { key: 'details', prompt: `YOUR FAILURE: B (DETAILS goes black). Reproduce it for Iron Crown, Pecharunt, Lokix and find which species do and do not fail (walk the national numbers; is it every added species, only some, and what they share -- a 5-line/4-line entry now paged? a picture? a cry? a height/weight comparison picture of the trainer?). Measure the Dex heap (its size, where it is created, its low water on each page) and find the allocation that fails or the assert that stops the DETAILS screens; compare with the same steps on a retail-size species (Bulbasaur). Say whether the paging change (it holds a 256-character String while an entry turns) contributes, by trying DETAILS on a species whose entry fits three lines.` },
  { key: 'search', prompt: `YOUR FAILURE: C (a search result fails a 34,516-byte allocation). Reproduce it; find the allocation's call site and what the 34,516 bytes are (a table sized by the species count? a picture buffer?), the Dex heap's size and use at that moment, what retail HeartGold allocated there for 493 species, and what the port's 1025 species need. Say whether B's failure is the same exhaustion. Propose the canonical fix: a heap sized for what the Dex now holds (and whether the parent heap has the room, measured in the field when the Dex opens), or a smaller allocation (the table built for the species actually listed), with the numbers.` },
]
phase('Diagnose')
const diag = await parallel(DIAG.map(d => () => agent(`${CONTEXT}\n\n${d.prompt}\n\nReport the root cause with evidence (assert text, addresses, allocation sizes, heap numbers, file:line), every other species/scene affected, and the fix you recommend.`, { label: `diagnose:${d.key}`, phase: 'Diagnose' })))

phase('Fix')
const fixed = await agent(`${CONTEXT}\n\nThe three diagnoses:\n${diag.map((d, i) => `=== ${DIAG[i].key}\n${d}`).join('\n\n')}\n\nYOUR JOB: in ${WT}, 'git checkout -b dex-bugs newgold' (newgold's tip; the worktree is clean on dex-pages, which is in newgold). Fix every confirmed cause at its source, canonically, in commits of one thing (decomp first where assembly must change; a heap resize only with the parent heap's room measured when the Dex is opened from the field and from the start menu, after a battle, and from the PC if it can be). Add tests that fail without each fix (host tests where the logic compiles on the host; data tests for importer/archive fixes). Build HeartGold, SoulSilver and the diagnostics build; with the harness show each failure gone: Enamorus's page, DETAILS on Iron Crown/Pecharunt/Lokix and on Bulbasaur, a search and its results page, and walk every species' Dex page and DETAILS if time allows (report any other failure found). Run test_boot and the full suite. Save screenshots under ${SCR}/fix/shots/. Report the commits, the md5s (and byte-identical proofs for decomps), the heap numbers before/after, the screenshots, and anything left.`, { label: 'fix:dex', phase: 'Fix' })

phase('Review')
const review = await agent(`${CONTEXT}\n\nThe fixer's report (branch dex-bugs in ${WT}):\n${fixed}\n\nYOUR JOB, adversarially and read-only (build in ${WT} if you must, commit nothing; scratch under ${SCR}/review): rebuild the three ROMs and confirm the md5s; confirm each decomp commit is byte-identical; reproduce each of the three failures on newgold's tip build (cc2c01c5b) and confirm each is gone on dex-bugs; walk the Dex on dex-bugs -- every species' Info page, DETAILS (size, cry, forms pages), the search with several filters and its results, the capture page after a catch -- and report any assert, failed allocation, black or garbled screen; check any heap resize against the parent heap's worst case (open the Dex from the field right after a battle and from the start menu with the bag and party opened before); check the paged entries still turn and stop as before; run test_boot and the full suite. Report findings marked critical/high/medium/low with evidence, or 'none'.`, { label: 'review:dex', phase: 'Review' })

return { diag, fixed, review }
