# Audit of 2026-09-23

Two reviews run on claude-opus-5-5 against the working tree: every place the
added species (508..1437) can overflow a table, archive or guard, and every
ported area against konefr's reference, each claim put to a refuter that
tried to disprove it. What is below survived. A row closes by a commit that
names it; the ledger's rows point here where they are affected.

## Found by playing the gyms headlessly

- **high** Whitney and Morty hang the battle. The battle asks msg_0197 for row
  1419 (tag nickname, the opposing battler), a bank that ends at 1414: the
  base row is 1417, the "The opposing ..." variant two on. In konefr's bank
  1417-1419 is "{0} is overflowing with space power!" -- Meteor Beam's charge
  line -- so something prints the reference's row number where this bank has
  no such row. The assertion fires in BattleMessage_ExpandPlaceholders and the
  text printer never finishes. Against Morty it comes on the first turn,
  right after Typhlosion's Flame Wheel on Misdreavus. -- open
- Falkner played to the end headlessly: the leader's Super Potion, Delibird's
  Focus Sash, the badge, the prize, and the party's experience held at the
  cap. Bugsy played to the end (the player's party lost to Scizor and
  Heracross, no error). -- verified

## Species past retail's tables

### archives

- `src/pokemon.c:2441` files/pbr/pokegra.narc (NARC 194) has 2964 members, which is 494 x 6, so it covers species 0..493 only. sub_02070560 is the default case of DP_GetMonSpriteCharAndPlttNarcIdsEx and has no bound. The working tree guards only the height half... -- open
- `src/field/scrcmd_move_tutor.c:156` waza_oshie.bin is 11480 bytes, which is 1435 records of 8 bytes. It leaves out the egg and the bad egg, so species s >= 496 sits at record s-3 (waza_oshie.json[505] is SPECIES_LILLIPUP). The working-tree fix returns species-1 for every... -- open
- `src/pokemon.c:4381` files/a/1/8/0 (NARC 180) has one member of 43966 bytes, which is 494 records of 89 bytes (species 0..493). The working tree clamps only NARC_ReadPokepicAnimScript (PokepicAnimSpecies). Its siblings sub_020729D8 (4381), sub_020729FC (4387)... -- open
- `asm/overlay_96.s:1039` The Pokeathlon course reads performance.narc (NARC 169, 554 members) through its own species->member table ov96_0221AAE8. That table is 0x3DC bytes, which is 494 halfwords (species 0..493). The working tree guards only the C copy in... -- open
- `src/pokemon.c:4375` Same archive a/1/8/0 (494 x 89-byte records). sub_020729A4 here and sub_0207294C (line 4365, a3 is the species) are unguarded, although their sibling NARC_ReadPokepicAnimScript was clamped to record 0. -- open
- `asm/overlay_112.s:7119` The Pokewalker app loads files/a/2/4/8 (NARC 250, 540 members) at icon index - 7 (ov112_021E9464 here, ov112_021E9470 at 7130). GetMonIconNaixEx now puts the added species at icon species-508+551, which is past everything this archive... -- open
- `asm/overlay_112.s:7296` ov112_021E9480 maps a species to a member of files/a/2/5/6 (NARC 258, 664 members) through ov112_021F397E. That table runs 0x021F397E..0x021F4138, which is 494 pairs of halfwords (species 0..493), and nothing bounds the species. -- open

### asm

- `asm/overlay_96.s:1037` The Pokeathlon course has its own species-to-performance-record table. It is still in asm and has no bound. ov96_0221AAE8 (asm/overlay_96.s:107517) holds 494 u16 entries, for species 0..493. The member it gives is read from... -- open
- `src/pokeathlon/pokeathlon_save.c:88` Pokeathlon medals are stored as one byte per species, in unk_DC[0x1F0] (496 bytes). ov96_021E8028 writes a medal bit for each of the three entrants, indexed by species-1, with no upper bound. The readers are not bounded either:... -- open
- `src/pokemon.c:2441` The dress-up portrait (overlay_41, ov41_02249604) draws the chosen mon through sub_02070130. That goes GetBoxmonSpriteCharAndPlttNarcIds(..., TRUE), then DP_GetMonSpriteCharAndPlttNarcIdsEx, whose default case is sub_02070560. That reads... -- open
- `asm/overlay_96.s:3244` When the player sets a new event best, ov96_021E786C saves each entrant's species in a 9-bit field of the Pokeathlon event record. The record viewer extracts it with `lsl r0, r0, #0x17; lsr r0, r0, #0x17` (asm/overlay_99.s:457-458) and... -- open
- `asm/overlay_112.s:7292` The Pokéwalker send path has a species-to-walker-sprite table with no bound. In ov112_021E9480, the default case indexes ov112_021F397E (asm/overlay_112_021F35A4.s:223), which is 1978 bytes: 494 entries of 2 u16. The result is passed to... -- open
- `asm/overlay_98.s:1409` The Pokeathlon medal totals only go through species 1..493. ov98_0221F024 counts species with all 5 medals, and ov99_021E6FA0 (asm/overlay_99.s:2819, same `ldr r7, =0x1ED` loop) counts all medals. Both are called at asm/overlay_99.s:2881... -- open
- `asm/unk_0202FBCC.s:1775` The battle-video header getter sub_0203088C, case 0, returns species 0 for any of its 12 party species above 493. -- open
- `asm/overlay_109.s:4064` The photo album counts the Pokémon in a photo as species 1..493 only. The caller at asm/overlay_109.s:3390-3393 uses the count to pick the caption string ([r5+0x4c] when the count is above 1, otherwise [r5+0x48]). -- open

### c-arrays

- `src/pokedex.c:81` The Deoxys form history is still kept in a fixed retail slot: bits 24..31 of word 15 of caughtSpecies and seenSpecies. That slot sat free above species 493, but it is now the seen/caught bit for species 505..512, which includes... -- open
- `include/application/pokedex/pokedex_internal.h:209` The Dex list inside PokedexAppData was sized to the retail count, and asm/overlay_18.s still hardcodes that count. Commit ea74ddf71 raised NATIONAL_DEX_COUNT from 493 to 1041 and grew this array by 548*4 = 0x890 bytes. Every C field from... -- open
- `include/pokeathlon/pokeathlon_save.h:46` The Pokeathlon medal table in the save has one byte per species, 0x1F0 = 496 entries, indexed by species-1. PokeathlonSave_SetUnkDC_AtIndex and GetUnkDC_AtIndex check only the lower bound. Every species from 497 to 1437 reads or writes... -- open
- `src/field/scrcmd_move_tutor.c:199` For every added species the tutor index is off by two. The file drops the egg and the bad egg, so the record for species s >= 496 is at s-3, but GetMoveTutorLearnsetIndex returns s-1. Species 1436 and 1437 index past the end of... -- fixed in 9eca47d8b
- `src/pokemon.c:4381` The per-species picture record table (a/1/8/0 member 0) was guarded only in NARC_ReadPokepicAnimScript through PokepicAnimSpecies (:4356). Its five sibling readers, which take the same member and the same species index, are unguarded, so... -- open
- `src/pokedex.c:578` The three Johto count loops now run i up to NATIONAL_DEX_COUNT (1041), but LoadSpeciesToJohtoDexNoLUT (src/pokemon.c:3428) returns retail's johtozukan member: 988 bytes, 494 halfwords, species 0..493. Every seen or caught species 494..1041... -- open
- `src/pokemon.c:2441` The other game's (pbr) picture archive has retail's six members per species, 2964 = 494*6. DP_GetMonSpriteCharAndPlttNarcIdsEx's default case sends every species here unguarded. This is the sprite sibling of... -- open

### guards

- `include/application/pokedex/pokedex_internal.h:209` The Pokedex app struct is sized with NATIONAL_DEX_COUNT (now 1041, was 493), so every PokedexAppData field after 0x878 sits 0x890 bytes later in C than in overlay_18 asm, which still uses retail literal offsets. -- open
- `src/pokedex.c:77` Deoxys's form order is kept in bits 24..31 of word 15 of caughtSpecies and seenSpecies. With NATIONAL_DEX_COUNT = 1041 (NUM_DEX_FLAG_WORDS = 33, include/pokedex.h:19) those bits are the real dex flags of species 505..512, so Lillipup... -- open
- `asm/unk_02030A98.s:1381` Battle Hall keeps one u16 streak record per species in three tables of 0x1EF (495) entries, at +4, +0x3E2 and +0x7C0 of an extra save chunk of 0xBA0 bytes (sub_020312A4). The writer indexes by species with no bound, and the script readers... -- open
- `src/pokeathlon/pokeathlon_save.c:88` The Pokeathlon medal bytes are u8 unk_DC[0x1F0], one per species. The setter only checks the lower bound, so an added species ORs medal bits into whatever save data follows. -- open
- `src/pokedex.c:577` The Johto LUT is retail poketool/johtozukan.narc: 988 bytes, 494 halfwords for species 0..493, the same limit SpeciesToJohtoDexNo now guards. These loops index it up to 1041. -- open
- `src/field/scrcmd_move_tutor.c:156` The uncommitted fix's comment says both species and forms are 'minus the two missing', but only the retail form cases subtract 2. Ordinary added species get index species-1 when waza_oshie.bin stores them at species-3. -- open
- `asm/overlay_98.s:1103` Pokeathlon course records store each record team's species in a 9-bit field (ov96_021E786C, `& 0x1FF`). The course-record screen reads it back (ov99_021E5C4C, `lsl #0x17 / lsr #0x17`) and prints it through ov98_0221EE28, which asserts... -- open

### importers

- `src/field/scrcmd_move_tutor.c:199` The move tutor reads the wrong record for every added species 508..1437. The record is two places off, and for 1436 and 1437 the read goes past the end of the file. -- fixed in 9eca47d8b
- `files/msgdata/msg/msg_0803.gmm:2296` Galarian Slowpoke (573) and Galarian Slowbro (574) show an empty Dex entry, "????.? lbs." and "???’??”". The real text is in the banks, but at positions the game never reads. -- fixed in fed164417
- `tools/newgold/import_dex_text.py:165` import_dex_text.py is not idempotent. Every run appends another 398 rows (573, 574 and 1042..1437) to each of the 27 banks. -- fixed in fed164417
- `tools/newgold/import_evolutions.py:170` Eelektrik (657), Charjabug (766) and Tadbulb (959) have no evolution entry at all, because the reference's ITEM_THUNDER_STONE is never mapped to this tree's ITEM_THUNDERSTONE. -- fixed in 419489cfe
- `tools/newgold/import_species.py:304` 324 of the 396 forms can learn no TM or HM (empty tms and hms in personal.json). 191 of them are not megas, Gigantamax or primals. The reference gives each of them its base species' machine moves. -- fixed in 419489cfe
- `src/pokemon.c:3149` No importer extends poketool/personal/pms.narc, the baby-species table. It is still retail's 1016 bytes (508 halfwords for species 0..507), so GetEggSpecies returns SPECIES_NONE for all 930 species 508..1437. -- open
- `src/pokemon.c:4375` Only NARC_ReadPokepicAnimScript was given the PokepicAnimSpecies clamp. Its five siblings read the same retail table without it: sub_0207294C (4365), sub_020729A4 (4375), sub_020729D8 (4381), sub_020729FC (4387) and sub_02072A20 (4393). -- open
- `files/application/zukanlist/zkn_data/zukan_data.json:20138` zukan_data.json disagrees with itself. mon_stats has 1438 records, but every sort list (national, alphabetical, heaviest, lightest, tallest, shortest) has exactly 493 species, and the Dex list is built only from those lists. None of... -- open

## Differences from konefr's reference

### abilities

- **high** *Comatose / Purifying Salt / Flower Veil / Pastel Veil / Water Bubble / Thermal...* -- Six abilities the test lists as IMPLEMENTED have no status-immunity half here. A Comatose Komala can be burned, poisoned, paralysed, frozen or put to sleep, and Hex does not double against it. Purifying Salt and grass-type Flower Veil... -- open
- **high** *Prankster Dark-type immunity* -- A Prankster-boosted status move aimed at an opposing Dark type lands here. In the reference it "doesn't affect" the target. Only the priority half of Prankster is ported. -- fixed in 93831a0b8
- **medium** *Queenly Majesty / Dazzling / Armor Tail: which priority they block* -- Here only a move-table priority above 0 is blocked. The reference blocks whatever adjusted priority the turn order used, which includes Prankster +1, Gale Wings +1, Triage +3 and Grassy Glide in grass. Because the reference stores it as a... -- fixed in 93831a0b8
- **medium** *Tinted Lens multiplier* -- In konefr's game Tinted Lens multiplies resisted damage by 1.25. Here it doubles it. -- open
- **medium** *Neutralizing Gas, Quick Draw, Supersweet Syrup: behaviour the reference doesn't...* -- In konefr's game these three abilities do nothing (the only reference reads of Neutralizing Gas are in Skill Swap/Role Play blocklists). Here Neutralizing Gas suppresses every other ability on the field, Quick Draw goes first 30% of the... -- open
- **medium** *Ice Scales stacking* -- konefr's game quarters special damage against Ice Scales in a single battle (two iterations) and cuts it to 1/16 in a double. The port halves it once. That breaks the port's own rule, which keeps the reference's Tablets/Vessel of Ruin and... -- open
- **medium** *Sand Rush / Sand Force sandstorm immunity* -- Sand Rush and Sand Force holders take sandstorm chip damage here. In the reference they are immune. Both are listed as IMPLEMENTED. -- open
- **medium** *Infiltrator: Safeguard and Mist* -- Here Infiltrator gets past substitutes and screens, but Safeguard still blocks its status moves and Mist still blocks its stat drops. In the reference it gets past all four. -- open

### battle-mechanics

- **high** *AI move scoring* -- The trainer AI is still assembly. It reads a move's power and effect at ctx+0x3E1 / ctx+0x3DE + move*16, i.e. trainerAIData.moveData[], which is sized NUM_MOVES+1 = 468 (MOVE_SHADOW_FORCE). For every added move (ids 468..923) the AI reads... -- open
- **high** *priority* -- The importer parses priority with `\d+` and drops the sign. Circle Throw and Dragon Tail have priority +6 here against the reference's -6, and Beak Blast and Shell Trap +3 against -3. So these phazing and charge moves act first instead of... -- fixed in 93831a0b8
- **high** *critical hits* -- Crit damage multiplier: ours is Gen 4 (x2, Sniper x3) as an integer multiply of the whole damage. The reference is Gen 6+ (x1.5, with Sniper an extra x1.5 final modifier, i.e. x2.25) and applies it after spread/weather, before the random... -- open
- **high** *critical hits* -- Crit-stage odds differ. Ours is Gen 4: 1/16, 1/8, 1/4, 1/3, 1/2. The reference is Gen 7+: 1/24, 1/8, 1/2, always, always. At stage 0 ours crits more often; at stages 2 and above the reference crits far more often, and is certain from stage... -- open
- **high** *speed* -- Paralysis quarters Speed in ours (Gen 4). The reference halves it (Gen 7+). -- open
- **high** *status* -- Burn residual damage is 1/8 max HP in ours (1/16 with Heatproof). The reference uses 1/16 (1/32 with Heatproof). -- open
- **high** *damage formula* -- Ours halves the target's Defense for MOVE_EFFECT_HALVE_DEFENSE (Explosion, Self-Destruct), which is Gen 4. The reference's Gen 5+ calc has no such halving even though the effect id is kept, so these moves hit about twice as hard here. -- open
- **medium** *damage formula* -- The damage pipeline is Gen 4 integer arithmetic in ours and a Gen 5+ Q4.12 chain in the reference, with a different order. Ours: base damage with burn, screens, spread and weather folded in before +2, then crit, then Life Orb/Metronome/Me... -- open

### config-toggles

- **high** *APPLY_ANTIPIRACY* -- The reference makes the overlay-1 card check return TRUE and neuters the DSProt checks in overlay 123, so the game runs on flashcarts (R4, TWLmenu). Our tree keeps HeartGold's anti-piracy whole: the card-status test, the... -- open
- **high** *Capture formula...* -- The ball-generation toggles sit inside a Gen-8/9 capture formula that we did not port. Our tree keeps HeartGold's Gen-4 formula with only the per-ball multipliers changed, so catch odds differ nearly everywhere. The reference has a... -- open
- **low** *IMPLEMENT_CRITICAL_CAPTURE (critical roll outcome)* -- When the critical roll hits in the reference, only one shake check runs and the result is returned without the critical mask, so the throw fails after one shake. Ours deliberately treats the roll as animation only. (already in the ledger) -- open
- **low** *FRIEND_BALL_GENERATION* -- In the reference, a Pokémon caught in a Friend Ball ends with 200 friendship: the second, unconditional block overwrites the generation-9 value of 150 on every successful catch. Ours sets 150. -- open
- **medium** *EXPERIENCE_FORMULA_GEN* -- The reference's scaled formula uses the truncated integer square root and divides by the number of participants after scaling. Ours uses a fixed-point root and scales after dividing, so every award where the two levels differ is a... -- open
- **medium** *HIDDEN_ABILITIES / HIDDEN_ABILITIES_FLAG / HIDDEN_ABILITIES_STARTERS_FLAG* -- In the reference, a script sets flag 2600 and the next generated Pokémon gets its hidden ability, stored as a persistent bit that survives ability resets and evolution; flag 2601 does the same for starters. None of this exists here: only... -- open
- **high** *MEGA_EVOLUTIONS / PRIMAL_REVERSION / FLAG_MEGA_EVOLUTION_ENABLED* -- Mega Evolution (enabled by flag 2518) and Primal Reversion are enabled behaviour in the reference and are not implemented here. The ledger defers them because the data is missing, but the mega stones, the orbs, Kyogre, Groudon and every... (already in the ledger) -- open
- **medium** *IMPLEMENT_SEASONS* -- In the reference, Deerling and Sawsbuck in the party take the current season's form, (month-1)%4, on the RTC update. That is not implemented here, and because our forms are separate species it would mean switching species. (already in the ledger) -- open

### decompiled-not-patched

- **medium** *asm edited instead of decompiled: thirty boxes* -- The thirty-box behaviour goes through 15 assembly sites in four overlays that were never decompiled. Commit 8a02692c2 swapped literal #0x12 for #NUM_BOXES while NUM_BOXES was still 18, and 668543b5d set it to 30 seven minutes later. From... -- open
- **medium** *asm edited instead of decompiled: battle script command table* -- The 69 added opcodes are dispatched through a table the port extended in assembly (commit dc0c05324 and later ones) rather than moving battle_command.c's rodata into C. The opcode names do match the reference one for one, so this is about... -- open
- **low** *asm edited instead of decompiled: Moon Ball list* -- The Moon Ball species table was rewritten from 14 entries to 6 inside the asm rodata (commit 2731f5f2a), and the C extern was resized by hand to match. The values match the reference, but the table's only definition is assembly, and its... -- open
- **low** *asm edited instead of decompiled: move relearner's type-icon offset* -- Commit 10e4a7f1d changes behaviour in an undecompiled relearner routine. The contest-condition icon index now starts at 19 instead of 18, done by editing the immediate in assembly. -- open

### encounters-world

- **high** *daycare / egg species (breeding added species)* -- The table that maps a mother's species to its egg species (pms.narc) is still the cartridge's 508-entry file here. Every species past it (Victini and all 532 added species the reference's BabyMons.c lists, plus the forms) gets egg species... -- open
- **high** *daycare / egg moves* -- Egg moves are still Gen 4's here. The list covers 198 species up to Weavile (459), so no added species can inherit an egg move. The reference has modern egg-move lists for 900 species, and all 198 species that overlap have a different... -- open
- **medium** *marts / price display* -- Ours shows mart prices with 4 digits; the reference patched this to 6. The imported prices put HP Up, Protein, Iron, Calcium, Zinc and Carbos at 10000, and they are sold at Goldenrod Dept. Store 4F (_020FBAFA) and Celadon 5F.... -- open
- **medium** *marts / specialty mart inventories* -- With MART_EXPANSION enabled (config.h:176), the reference repoints all 30 specialty-mart slots to its own lists. Three disagree with ours: (1) the two Goldenrod Dept. Store 2F clerks are swapped: SpecialMartBuy 3 sells Poke Balls, Repels... -- open
- **medium** *field / following Pokemon overworld sprites* -- In the reference every species, the added generations and forms included, has its own overworld sprite that walks behind the player. Here any species past Arceus, and every form species from 1042 up, resolves to offset 0 and walks as a... -- open
- **medium** *gift / wild / egg / starter Pokemon: hidden-ability script flags* -- The reference lets a script set flag 2600 so that the next Pokemon made gets its hidden ability: a wild or scripted static encounter, a givemon gift, a give_egg egg or the Togepi egg. Flag 2601 does the same for the starter, and an egg's... -- open
- **medium** *daycare / Mirror Herb and same-species egg-move sharing* -- When the second Pokemon is deposited, the reference copies egg moves between the two parents into their empty move slots if either holds a Mirror Herb or both are the same species (a Gen 9 mechanic). Ours only strips the Griseous Orb and... -- open
- **low** *field / seasons (Deerling, Sawsbuck)* -- In the reference, on the same real-time-clock tick that returns Shaymin to Land Forme, every Deerling and Sawsbuck in the party takes the form for the current month (month-1 mod 4). Ours has the species and their four season forms as... (already in the ledger) -- open

### items

- **critical** *item data loading (every GetItemAttr on an imported item)* -- Commit d02b649f1 reduced sItemNarcIds to 564 rows and sent ids 564 and up through GetItemIndexMapping. LoadItemDataOrGfx was not updated and still indexes the short table with the raw item id, so for every imported item (564..2692) it... -- fixed in c708be76a
- **high** *item price (20-bit) record layout* -- The price nibble is written at one offset and read at another. The data puts it at 0x20; the reader takes byte 0x22. That byte is outside the 34-byte record: it is the archive's 0xFF alignment padding in LoadAllItemData's bulk copy, and... -- fixed in c708be76a
- **high** *TM/TR machines past HM08* -- The data gives 340 items fieldUseFunc 6 (the TM routine), in both trees. Here only the 100 retail machines map to a move. The other 240, including TM093 (Flash Cannon), TM100 (Confide) and TR00..TR99, reach field_use_item.c:364... -- open
- **high** *hold effects with a reference consumer and none here* -- About 27 imported items carry a hold effect that does something in the reference and nothing here. A Gem is never used up and gives no ×1.3. Red Card, Eject Button and Eject Pack never force a switch. The origin Crystals, Blank Plate and... (already in the ledger) -- open
- **medium** *Pixie Plate / Arceus Fairy* -- An Arceus holding a Pixie Plate stays Normal here: its form, its battle type and Judgment's type. In the reference all three become Fairy. Only the ×1.2 Fairy move boost was ported (overlay_12_0224E4FC.c:7513). -- open
- **medium** *Ability Patch (party use)* -- In the reference the Ability Patch switches a Pokemon to or from its hidden ability. Here its record (fieldUseFunc 1, partyUse 1, no party-use bits set) falls through to CanUseItemOnMonInParty, which finds nothing to do, so it has no... -- open
- **medium** *field-use routines 30/31/34/35* -- Seven items (four Nectars, Reveal Glass, DNA Splicers, Rotom Catalog) sit in the bag and do nothing here. In the reference they open the party menu and change form. The 21 Mints and the Ability Capsule now have routine 1 and their own... (already in the ledger) -- open
- **medium** *Soul Dew* -- Soul Dew uses the Gen 4 rule here: ×1.5 Sp. Atk and ×1.5 Sp. Def, except in the Frontier. In the reference it uses the Gen 7+ rule: ×1.2 power on Dragon and Psychic moves only, and no defence boost. -- open

### level-cap-exp

- **high** *experience formula (scaled, EXPERIENCE_FORMULA_GEN = GEN_LATEST = 9)* -- The reference truncates sqrt(2L+10) and sqrt(L+Lp+10) to whole numbers; ours computes them almost exactly. Almost every experience award therefore differs from the reference's. -- open
- **low** *experience split order and minimum of 1* -- Ours divides and applies the minimum of 1 before scaling, then scales the combined participant and Exp. Share amount once. The reference scales first and then divides each part with its own minimum of 1. The results can differ by rounding... -- open
- **medium** *effort values at the cap once the cap is 100* -- After Morty the reference's cap is 100, so a level-100 Pokémon still gains effort values from battles. Ours excludes cap == MAX_LEVEL, so a level-100 Pokémon gains none, which is vanilla behaviour. -- open
- **low** *effort values for a fainted Pokémon at the cap* -- A capped Pokémon with 0 HP that holds an Exp. Share, or still has its participation bit (for example it went down in the same turn as the foe), gets effort values in the reference but not in ours. -- open
- **medium** *per-stat effort-value cap in battle (and Frontier Pokémon)* -- Battle effort-value gains stop at 252 per stat in the reference and at 255 in ours. The same byte patch lowers the Frontier Pokémon clamp to 252, which ours keeps at 255. -- open
- **low** *Rare Candy repeat use* -- In the reference, when a Rare Candy does not trigger an evolution and more are in the bag, the party menu stays open on 'Use on which Pokémon?' so the next candy can be used straight away. Ours always goes back to the bag. -- open
- **low** *Rare Candy at level 100 (evolution): friendship* -- Using a Rare Candy on a level-100 Pokémon that can evolve raises friendship (+5/+3/+2) in ours but not in the reference. Everything else about it matches: the candy is accepted, consumed, and the evolution is found in case 6. -- open

### move-effects

- **high** *move data: priority* -- Every added move with negative priority is written with the sign dropped. Circle Throw and Dragon Tail are +6 here (the highest priority in the game) instead of -6. Beak Blast and Shell Trap are +3 instead of -3. -- fixed in 93831a0b8
- **high** *move data: effect (Triple Dive)* -- Triple Dive was given Triple Kick's effect. Past effect 276 the importer matches effects by name, and pret's name for Triple Kick's effect 104 is the same as the reference's name for its flat three-hit effect 299. So Triple Dive checks... -- fixed in 93831a0b8
- **high** *move effect: Solar Seeds (konefr's own move)* -- Solar Seeds always hits exactly twice. In the reference it hits 2-5 times, with Skill Link and Loaded Dice applying. -- open
- **medium** *move effect: 2-5 hit distribution* -- Multi-hit moves use Gen 4 odds here (2 and 3 hits 37.5% each, 4 and 5 hits 12.5% each) instead of the reference's Gen 5+ odds (35/35/15/15). This covers Bullet Seed, Pin Missile, Rock Blast, Scale Shot and the rest. -- open
- **medium** *move effect: Triple Axel* -- Triple Axel hits 20/20/20 here instead of 20/40/60. -- open
- **high** *move data: FLAG_UNUSED_MOVE (bit 5) not enforced* -- The reference marks 79 moves as unusable. They are skipped when learnt by level, removed from trainer parties, and cannot be selected in battle. Ours reuses the bit for King's Rock and has no such gate, so all 79 can be learnt and used. In... -- open
- **medium** *King's Rock / Stench flinch eligibility* -- King's Rock works on different moves here. By the reference's rule, 152 moves can flinch with it that cannot here (85 of them retail: Thunderbolt, Flamethrower, Ice Beam, Psychic, Body Slam, Fire Punch...). 53 can flinch with it here that... -- open
- **high** *Protect variants: contact penalties and status moves getting through* -- All six moves exist in this game, but here they are plain Protect. There is no penalty for making contact, and the four that should let status moves through block them too. -- open

### species-forms

- **high** *form resolution (PokeFormDataTbl / GetSpeciesBasedOnForm)* -- Ours has no run-time mapping from (base species, form number) to the form's record. In the reference every lookup goes through PokeOtherFormMonsNoGet/GetSpeciesBasedOnForm: personal data, experience, hidden ability, TM, tutor and level-up... -- fixed in 9eca47d8b
- **high** *TM/HM compatibility of forms and level-up TM moves* -- The reference gives a form with no MachineMoves of its own its base species' TM list, and makes every TM whose move the species learns by level-up compatible. Our importer does neither. As a result 324 of the 396 forms here have tms [] and... -- fixed in 419489cfe
- **high** *Mega Evolution and Primal Reversion* -- Both toggles are enabled in the reference, and neither mechanic exists here. The ledger defers them on grounds that are no longer true. The 98 mega stones are items here, Kyogre and Groudon are species (and always were in HGSS), their... (already in the ledger) -- open
- **high** *in-battle form changes (ability- and move-driven)* -- None of the reference's post-Gen-4 battle form changes exist here. The form species' records (e.g. DARMANITAN_ZEN_MODE, AEGISLASH_BLADE, WISHIWASHI_SCHOOL, MIMIKYU_BUSTED, MINIOR_CORE_*) are present but nothing ever switches a battler to... (already in the ledger) -- open
- **medium** *passive wild forms (female forms, 1% forms)* -- In the reference, half of all wild Unfezant, Frillish and Jellicent are generated in their female form (which is 100% female), and 1 in 100 wild Dunsparce and Tandemaus carry form 1 into Dudunsparce Three-Segment and Maushold Family of... -- open
- **medium** *battle-start forms and form reversion* -- The reference forces Xerneas Active and crowns Zacian or Zamazenta holding the Rusted Sword or Shield (with the move swap) at the start of every battle. At switch-out and battle end it reverts every NEEDS_REVERSION form to the form listed... (already in the ledger) -- open
- **low** *reversion side effect: bad poison at battle end* -- The reference's reversion routine also turns badly poisoned into regularly poisoned for the whole party at battle end, before any form check. Here a badly poisoned Pokemon stays badly poisoned into the next battle. -- open
- **medium** *hidden-ability grant at run time (HiddenAbilityTable use)* -- In the reference, a script sets flag 2600 before a wild battle, a gift or an egg, or flag 2601 before the starter, and that Pokemon gets its hidden ability. A persistent bit then re-applies the (form-adjusted) hidden ability after... -- open

### tests-honesty

- **high** *battle / protect-variant moves* -- Wide Guard, Quick Guard, Crafty Shield and Mat Block (move effect 373) set no protect flag at all, so they protect nothing. King's Shield, Spiky Shield, Baneful Bunker, Obstruct, Silk Trap and Burning Bulwark (effect 111) are plain... -- open
- **high** *tests / move-effect and battle-command ratchets* -- The STILL_TO_DO=0 ratchet counts script shapes, not behaviour. Once an effect script contains anything besides a bare hit, removing all of its C still leaves the test green. The STUBS ratchet only checks that no script here runs a stub, so... -- open
- **medium** *battle / Hurricane and the three Storms accuracy* -- Hurricane (effect 345), Bleakwind Storm (360), Wildbolt Storm (361) and Sandsear Storm (362) keep their normal accuracy in rain, where the reference never misses. Hurricane also keeps it in sun, where the reference drops it to 50. -- open
- **medium** *tests / what counts as an implemented ability* -- Schooling and Power Construct are counted as having an effect only because their names appear in the Skill Swap, Role Play, Worry Seed, Gastro Acid and Simple Beam ban-lists. The form changes konefr implements for them in C are absent... -- open
- **medium** *ratchets / species-in-play and pending abilities after the forms import* -- The species ratchet was raised from 18 to 58 in the forms commit with no reason written down, while its own comment says it may only come down. The comment above SPECIES_IN_PLAY and the docstring still describe the 1027 range. The ledger's... (already in the ledger) -- open
- **medium** *items / form-change items pinned inert* -- The test pins konefr's four form-change item routines as doing nothing, citing missing forms. After today's import those forms exist: SPECIES_TORNADUS_THERIAN 1171, KYUREM_WHITE/BLACK 1174/1175 and ORICORIO_POM_POM 1239 (the Rotom forms... (already in the ledger) -- open
- **high** *move tutor / species index (committed today, 16955ee61)* -- Every species from 508 on reads the tutor record two places later. Lillipup (508) gets Stoutland's record 507, and so on. Species 1436 and 1437 seek to byte 11480 and 11488 of an 11480-byte file, so FS_ReadFile reads nothing and the... -- fixed in 9eca47d8b
- **medium** *battle / RESTORE_ITEMS_AT_BATTLE_END* -- We differ from konefr in three ways. (1) A Kee, Maranga or Roseli Berry eaten in battle comes back afterwards, because the berry range stops at Rowap. Kee and Maranga are eaten on a hit here (overlay_12_0224E4FC.c:6500-6507), so they... -- open

### trainers-gyms

- **high** *trainer AI / move data* -- konefr's AI scores every move from the full move table. Ours reads `ctx + 0x3DE + move*16` with no bound, so for any move numbered above 467 it takes power, type and effect from whatever BattleContext fields sit after moveData. The... -- open
- **high** *trainer Pokemon personality: ability slot translation* -- The PID modifier carries over from one party member to the next in both engines, but the translated bytes do different things to it. (1) konefr's HIDDEN slot 0x02 also forces the personality to genderRatio-2, and that stays in force for... -- open
- **high** *platform capacity: species on a trainer* -- No species numbered 1024 or above can be a trainer Pokémon here: 18 species (Iron Valiant through Pecharunt) and all 396 form species. The mask wraps it, so SPECIES_KORAIDON (1025) is read as species 1, form 1. konefr's engine takes any... -- open
- **medium** *trainer Pokemon personality: species numbers* -- 30 species konefr's trainers use have different numbers here, so their personality seed and nature differ from his build. In scope: Morty's Annihilape (Bashful→Jolly), Whitney's Farigiraf (Calm→Adamant), Carrie's Herdier, Al's Joltik,... -- open
- **medium** *trainer moves: BLOCK_LEARNING_UNIMPLEMENTED_MOVES* -- In konefr's build, Morty's Annihilape (Trainers.c [31]) enters with Bulk Up / (empty) / Drain Punch / Taunt, and Issac's Whismur [391] loses Echoed Voice. Ours gives both the move. Trainers.c holds 79 moves flagged this way, so any trainer... -- open
- **medium** *trainer names: length limit* -- {TRNAME} names stay packed at 9 bits per character plus a marker. 'Pippo Franco' packs to 9 units and 'Pietro Pacciani' to 10, so both fit konefr's 12 but not our 8. In a clean build, CopyStringToU16Array asserts and copies nothing, so the... -- open
- **medium** *trainer names: stale build of the name bank* -- The working-tree build names Route trainers 47 and 383 Mikey and Peter, where konefr's are Pippo Franco and Pietro Pacciani. It also still has the vanilla '{TRNAME} -' (0) and bare 'Cheryl'…'Darach' (667-671, 707-711). Editing... -- open
- **medium** *trainer dialogue* -- konefr's dialogue for three trainers is missing. In battle, ours' Proton (Slowpoke Well) says vanilla LAST_POKE and LAST_POKE_HALF lines konefr removed, and a different defeat line. Pippo Franco and Pietro Pacciani speak vanilla Mikey and... -- open

