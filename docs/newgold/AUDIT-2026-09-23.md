# Audit of 2026-09-23

Two reviews run on claude-opus-5-5 against the working tree: every place the
added species (508..1437) can overflow a table, archive or guard, and every
ported area against konefr's reference, each claim put to a refuter that
tried to disprove it. What is below survived. A row closes by a commit that
names it; the ledger's rows point here where they are affected.

## Found by playing the gyms headlessly

- **high** Whitney and Morty hang the battle. The battle asks for row 1419
  (tag nickname, the opposing battler) and the reader asserts; the text
  printer never finishes. The first reading of it was wrong: it is not
  msg_0197 and not a reference row number. The printer is ov12_0225A85C, the
  task that builds "X used Y!" as row 3 * move + side of msg_0003, and that
  bank stopped at Shadow Force, 467 -- `import_moves.py` wrote the names,
  capitals and descriptions of every added move and never this bank. Morty's
  Misdreavus uses Hex, 473, and 3 * 473 = 1419; any move past 467, on either
  side, stopped a battle the same way. -- fixed: the importer writes the
  three rows for all 456 moves past retail, as the reference's movedatagen
  does, and `test_message_banks.py` holds the bank to the move count. Morty
  and Whitney now play to the end with no assertion.
- Falkner played to the end headlessly: the leader's Super Potion, Delibird's
  Focus Sash, the badge, the prize, and the party's experience held at the
  cap. Bugsy played to the end (the player's party lost to Scizor and
  Heracross, no error). -- verified

## Second audit: retail limits the expansion crossed

Asked for by Paolo the same evening and run by the other session: six
finders looking for more bugs of the ledger's silent family -- a limit
retail baked in, crossed by the expansion, failing with no build or test
error -- then every item re-derived here by an adversarial verifier on
8b0eb2d7e. Two did not survive and are not rows: the Wi-Fi Plaza footprint
table (every path clamps species past 493 to Ditto before reading it) and
move target 24 (Rototiller, Flower Shield and Teatime take the same branches
as in the reference, where they are marked unusable).

- **critical** *FIGHT button of a Fairy move* -- `ov06_0221BA00` indexed an 18-entry palette table by the move's type; Fairy (18) read the 0 after it and loaded a palette from address 0. A data abort on hardware as FIGHT opens with any Fairy move (a wild Lillipup on Route 34 has Baby-Doll Eyes). -- fixed in 62b0da55e
- **critical** *Dex and capture type icon of a Fairy species* -- `ov18_021F967C`/`ov18_021F9688` indexed 18-entry tables; Fairy loaded zukan_gra member 0, a screen file, as the icon: NULL character data on the capture page, a read of 0x14 on the Dex page. -- fixed in bc8e8908b and 38d79dcb8
- **medium** *evolving into a form species registers nothing* -- `DexSpeciesIsInvalid` (src/pokedex.c) rejects species past NATIONAL_DEX_COUNT and nothing maps a form to its base; a female Litleo evolving into PYROAR_FEMALE (1312) leaves Pyroar unseen and uncaught, while the unique-caught stat still counts it. Needs a form-to-base table from the importer and one lookup in the Dex's seen/caught paths. -- fixed in 6f58a88f6
- **medium** *Battle Frontier abilities in a byte* -- `FrontierMon.ability` (include/scrcmd_9.h) is a u8 written from the u16 ability and written back with SetMonData: Weezing's Neutralizing Gas becomes none, Gallade's Sharpness Trace, Sudowoodo's Irrigation Minus. Bit 31 of the IV word is free for the high byte. -- fixed in 499ff7de9
- **medium** *Metronome rolls moves 1..467 only* -- `BtlCmd_Metronome` takes `% 0x1d3`; the reference rolls over all its moves, with its ban list widened for the added uncallable ones. -- fixed in 0ac85fa23
- **medium** *battle videos with added content are refused, playback stops at a move past 467* -- `sub_0203018C` (asm/unk_0202FBCC.s) checks species, items and moves against retail's limits, and `ov12_0225E4EC` against 0x1D3. Both need a matching decompilation first. -- fixed in e87865d7f (matching) and 4b42a09c0
- **low** *field abilities in a byte* -- `EncounterGenState.ability` (src/field/encounter_check.c) and `GetEggCyclesToSubtract` (src/get_egg.c) keep the ability in a u8, so an ability past 255 acts as a retail one: Farigiraf's Cud Chew (291) doubles the encounter rate as Illuminate, Armor Tail (296) halves egg cycles as Magma Armor. -- fixed in 46d9c9c52
- **low** *National Dex completion needs 1018 species* -- ea74ddf71 made `Pokedex_NationalDexIsComplete` count 1..1041 less the gap and nine mythicals; Oak's rating still calls the Dex complete past 483, while the diploma and the trainer card's star can never be earned. The reference leaves the completion at retail's count. -- fixed in f8ececb71
- **low** *AI replacement picker reads retail-sized move data* -- `ov12_02258800` reads a move's power by raw offset into `moveData[NUM_MOVES + 1]`; for an added move it reads an unrelated context byte. Needs a matching decompilation first; lands with the overlay-10 AI rows above. -- fixed in 7a3cb630e (matching) and c9311e949
- **low** *Pokemon Talk never mentions an added species* -- src/application/pokegear/radio/shows/pokemon_talk.c filters encounter slots by `MAX_SPECIES` (493). -- fixed in be9922c3a
- **low** *photo caption counts species 1..493* -- src/application/view_photo.c:480, the C twin of the open overlay-109 row. -- fixed in e252d3cdb (the C viewer; the overlay-109 twin is its own open row)
- **low** *mail shows every added species as the placeholder icon* -- src/mail.c clamps an icon index past 546 to 7. -- fixed in 76f548f36
- **low** *Battle Castle summary names the wrong ability past 255* -- `Ov83MonSummary.ability` is a u8; the battle itself uses the full ability. -- fixed in 78a539fda
- **low** *evolution record read into 48 bytes, 50 written* -- the template's `u16 pad0` after eight evolutions makes each evo.narc member 50 bytes, and both readers size 48; two zero bytes land past the buffer on every evolution and Eviolite check, harmlessly by luck of layout. -- fixed in be2573c48
- **low** *Trainer House record keeps the ability in a byte* -- include/save_trainer_house.h; reached only through an IR record. -- fixed in cde578b69
- **low** *Wi-Fi Club refuses a partner with an added species or item* -- src/overlay_44_0222CDAC.c `ov44_0222DFEC` keeps retail's bounds. Online only. -- fixed in 549cdc727
- **low** *update_vanilla_species.py --write would erase 451 hidden abilities* -- it rebuilds each record with the hidden ability defaulted to none. -- fixed in 3a96373ca
- **medium** *an evolution never teaches its evolution move* -- 272 learnset entries at level 0 in 268 species (two of them konefr's: Ariados' Swords Dance, Sudowoodo's Slam) mean "learned on evolution" in the reference, which keys on the evolution scene's call; here `MonTryLearnMoveOnLevelUp` only matches the current level. The evolution scene (`sub_02075E14`, asm/unk_020755E8.s) needs a matching decompilation first. -- fixed in 8042523bc (matching) and c1f868514
- **low** *no machine bit for a move learnt by level-up* -- the reference's build_learnsets.py also sets a species' machine bit when it learns the machine's move by level; no importer here does, so Delibird cannot take TM51 Roost, Sudowoodo TM81 X-Scissor. The machines past HM08 are inert either way (the open items row). -- fixed in 307fa1ef0 (engine) and f4db797d2 (konefr's Delibird and Sudowoodo)
- **low** *Dex number '?12' past 1000* -- the summary and the PC format the Dex number with three digits; a female Pyroar or Meowstic (1312, 1313) prints '?12' with the National Dex on. The reference widens the six digit counts to four; the root cause is the same missing form-to-base map as above. -- fixed in 311eca757 for every species a player can have: a form prints its base species' number (female Pyroar 549); the four-digit widening is its own open row
- **low** *bag lists 165 items, 24 balls, 50 key items* -- overlay 15's pocket counts and its 165 name strings are retail's while the save pockets were widened; not fillable by today's content. Needs the bag's list builder decompiled. The list builder and its strings are C since f86d8d6fe (matching); the redraw ov15_021FF364, ov15_021FF4EC, ov15_02200140, Bag_Init and the two size tables are left, then the reference's sizes. -- fixed in 81596de42 and 29d43db7b; the Items pocket's 197 slots seen listed and named in the harness
- **low** *jsonproc archives do not depend on their headers* -- personal, evo, trainer, encounter and tutor rules list only their JSON and template; a changed constant or record layout leaves the archive stale in an incremental build. All six rebuild byte-identical today. -- fixed in 0450c7d6f (the gaps it found are their own open row)
- **low** *Dex list builder sized for 493* -- `ov18_021F7ED4` and its neighbours in overlay 18 keep 493-entry scratch lists, a 518-slot grid and a 494-byte filter file; latent until the sort lists grow, with the open sort-list and PokedexAppData rows. The route, from round 5's reading: the two PokedexAppData lists become heap buffers of NATIONAL_DEX_COUNT as the reference's pokedex.s does, after seventeen overlay-18 accessors are decompiled matching; the Dex heap, zukan_hw_data_1 and the grid's three tile digits follow. -- fixed in 61cb37ad9, 0392bbab1, c131074b9, d0033e401, cb73ad79b and 806f55fbf; with the other fourteen matching accessors of that round; three that read only the counts stay asm on purpose
- **low** *Pokeathlon medal viewer asserts species <= 493* -- `ov99_021E72C0`; latent until the sort lists grow, with the open medal rows. -- closed (Paolo, 2026-09-23): added species do not earn Pokeathlon medals for now; a medal won by one is not recorded
- **low** *egg moves searched in 2045 halfwords, 16 a species* -- src/get_egg.c; latent until the egg moves are imported (the open daycare row). -- fixed in 0d9b59745
- **low** *Pokewalker receive path clears species past 493* -- five checks in overlay 112 the reference raises; IR only, and the send side is broken first (the open Pokewalker rows). -- out of scope (Paolo, 2026-09-23): the Pokewalker needs infrared, which no emulator or flashcart has; the plan is in the row
- **low** *two obtainable items' plurals do not fit* -- hg-engine's plurals 'pieces of Never-Melt Ice' and 'doses of Secret Medicine' are 33 units against the 32 of a MessageFormat buffer; tossing more than one Never-Melt Ice shows the bag's previous word. The reference has the same text and buffer. `test_text_length.py` lists both as known, so a third fails. -- fixed in c99abb1e3
- **low** *four-digit Dex numbers* -- species 1000..1041 print '?xx' with the three digits six sites format; the reference widens them to four. Needs sub_0208CC88, ov14_021F5190, ov18_021EE75C, ov18_021EEC34 and ov18_021F8CCC decompiled first. No player can have those species yet. -- fixed in 03f377af4, 551ba0d5e, 56e351cf8, c66e33f80 (matching) and fd6571659; seen in the harness as 'Dex No. 0023', 'No.0023' and '0025 Pikachu'
- **low** *build rules the jsonproc fix did not reach* -- zone_event bins do not depend on the headers their json names; safari_enc.mk's explicit rule writes '.s' and '.o' in the repository root; personal.mk has a dead line; clean-zukan-enc leaves the next full build failing until zukan_enc.naix is regenerated. -- fixed in bb85fc556, d27362d46, cf1eca6a8 and d27d143fa
- **low** *Shellos and Gastrodon East Sea register as West* -- form species map to their base with form 0 now; no encounter, trainer or script gives either. -- fixed in c6c6751e0
- **medium** *retail moves: what is still not hg-engine's* -- after 069b260e4 the retail records carry hg-engine's type, power, accuracy, PP, chance and named flags, with these left: Beat Up's power (retail's formula multiplies it), Sweet Scent's and Howl's effects (the scripts are retail's stubs or single-target), seven priorities (Teleport, Protect, Detect, Endure, Extreme Speed, Fake Out, Follow Me) and four targets (Poison Gas, Cotton Spore, Conversion 2, Howl); Knock Off has hg-engine's 65 but not its x1.5 against an item, Rapid Spin its 50 and 100% but not the Speed raise. -- fixed in 39525f093, 718e227e5, 4e1735d4c, 0de9d864e, d569bcb95, 84e2ae29c, fdbacf8e4 and 770e8b666; Conversion 2 keeps its retail target on purpose, because the engine's spread target with Gen 4's command would hit every neighbour -- a row of its own
- **medium** *retail effect scripts differ between the two trees* -- about 120 of effect scripts 0..276 differ in text at d0380a487: some only in flag order, many for real -- the stubs 21/22/61/63/64 filled with stat drops, 20 made a two-stage drop, Explosion/Damp, the binding moves, Rapid Spin and Beat Up reworked. import_moves.py's premise that they are "the same file in both trees" holds only in part. Fifteen brought over in 4c2036ec5, cd8841b33, abc652f92, 3f3b14aa3, 9159c3f40, 9c5ba5921, ddefec441, 8b65c721b, d569bcb95 and ff8fa9e34; c44f17475 compares the rest, and the ones still different are STILL_DIFFERENT in test_retail_effect_scripts.py, each with what it waits on -- the engine's C passes, the entry-hazard queue, Future Sight. Round 6 brought ten more (the hazard queue and the moves' power worked out with the damage: 307d2db57, 84854579a, 370835844, 608cff102, a49e3810a, f8fb6c5a7 and 1efb8032b); 36 are left, each with its real reason (40ef777e6 and ce05027fe), most waiting on the engine's post-move C. -- open for four: the seventh round made the engine's post-move steps C, in its order (recoil, the additional effects, Magician, the Red Card and Eject Button, Pickpocket, Emergency Exit, U-turn's switch), and brought Reckless and the recoil moves, Smelling Salts and Wake-Up Slap, the binding moves, Knock Off, Thief and Covet, Pluck and Bug Bite, U-turn and Fling over (8310f2152 to 54868614f), 36 left to 23; 19 of them are deliberate differences written down with their reason, and four wait on C outside the post-move pass: 7 Explosion (Damp before the move, the faint in the post-move step), 151 and 272 (the charge turn and the Power Herb), 222 Natural Gift (its power, a failure check before the script, the Berry spent on a miss too)
- **low** *item names longer than their buffers* -- 74 item names are longer than the bag's 18-unit list strings, 16 than the trade screen's 20, 31 than the Pokewalker's 19; 237 descriptions than the bag's and shop's 130, 64 plurals than the 32-unit MessageFormat. A copy that does not fit is refused and the screen keeps the previous string. None of those items can be obtained today. The bag's list names, the plurals and the three description buffers were widened in 802579d70, b7791a5c7 and 243be6fda (after f86d8d6fe and b3f8af7f0, matching); the trade screen's String_New(0x14) in ov65_0221D674, still asm, is what is left. -- fixed in 404e91e38 and 555c41663; with the bag's, the plurals' and the descriptions' of round 5
- **low** *heap margins after the per-species tables grew* -- `PokedexData_LoadAll` takes 23,008 bytes (retail 7,904) from the battle heap at every switch-in and from the field heap in the regulation menu. No allocation failed through four gym fights, but nothing measures the margin. -- closed: measured in d69344404 -- the battle heap keeps 240,300 bytes at its fullest in Morty's fight against the 23,008 a switch-in loads, FIELD2 64,656; the heaps that are close are a row of their own

## Found while fixing, 2026-09-23 night

What the agents of the third round found in passing, re-read here before
being written down. Each is engine layer unless it says otherwise.

- **critical** *four status subscripts longer than the script buffer* -- the third round's immunities grew Bad Poison, Poison, Fall Asleep and Burn past retail's 1600-byte battleScriptBuffer, so every load wrote into the fields after it; caught by the size assertion in the gym replay (Falkner's Toxic Orb). -- fixed in c9b5c2ec9, hg-engine's 650-word buffer at the end of BattleContext
- **critical** *opening the PC wrote 12 KB past its allocation* -- the box app's graphics state was retail's 0x88E0 bytes while thirty box pictures of 0x400 run from +0x40C8 to 0xB8C8, over the state's last fields and into what heap 10 allocates next; hg-engine grows the state and moves the pictures. -- fixed in ba1936a9a (matching) and 1d92c1b9e; whether heap 10 has room for the larger state is its own row
- **low** *heap 10 and the larger PC state* -- the state grew by about 30 KB and heap 10 (0x80000) did not, as in the reference; not yet opened on an emulator. -- closed: measured in d69344404 -- 238,612 bytes left at its fullest, the PC opened in Violet City and paged through
- **medium** *the Safari Game counted 18 boxes* -- BtlCmd_CheckSafariGameDone compared the first box with room with a literal 18. -- fixed in 3f9864d55
- **low** *the trainer AI's protect-chain command counts three moves* -- ov10_0221EBAC loads a chain only after Protect, Detect or Endure, not after the shields or the team guards. -- fixed in c91754f56 (matching) and 2116e7600
- **medium** *CheckSubstitute ignores Infiltrator* -- the reference's command lets an Infiltrator user through for direct and move-effect side effects (not Transform or Sky Drop); here Leech Seed, Gastro Acid, Worry Seed, Nightmare, Simple Beam and the status subscripts still stop it. -- fixed in 9b7619b5f and fb7bed50a
- **low** *Heal Block's target-side half* -- the reference refuses Heal Pulse on a heal-blocked target and Pollen Puff on a heal-blocked ally. -- fixed in 8033654af
- **medium** *resist berries outside the damage chain* -- subscript_0264 halves the damage after the unhalved value was stored for Counter, Mirror Coat, Metal Burst, Shell Bell and the Endure and Sash clamps, and takes the berry's type from the move rather than the adjusted type (Pixilate and the others, Liquid Voice, Ion Deluge); the reference halves inside the chain (6.9.13) and eats the berry before the hit. -- fixed in 6e136f163
- **low** *the final modifier's order between battlers* -- the reference visits the battlers by raw Speed; FinalDamageModifier takes attacker, target, ally, the same order whenever the attacker is faster. -- fixed in bb7c5c278
- **low** *Minimize doubling by list* -- the reference doubles Body Slam, Stomp, Dragon Rush, Steamroller, Heat Crash, Heavy Slam, Flying Press, Malicious Moonsault and Supercell Slam against a Minimized target and makes them always hit; here only effect 150 doubles. -- fixed in a86d5b3e6
- **low** *Spit Up has no damage roll* -- CalcDamageRaw keeps Gen 4's; the reference rolls. -- fixed in c8dc5d418
- **low** *Future Sight is worked out when used* -- the reference works it out when it lands. Round 5 left it: the landing has to keep the user's party slot, run DamageCalcDefault with the user in or its party stats when gone, and teach subscript 121 the chart. -- fixed in 70ec071a9; a Disguise, Ice Face or Tera Shell is not yet asked at landing
- **low** *Stuff Cheeks makes Recycle and Harvest forget the Berry* -- the subscript's second RemoveItem leaves recycleItem empty. -- fixed in e3121b4a4
- **low** *once-per-battle entry abilities are shared by multi-battle partners* -- onceOnlyEntryAbilityDone is by side and party slot, so the partner's slot 0 blocks the player's. -- fixed in c0465e637
- **low** *a Berry flung at a Cud Chew Pokemon is not kept* -- TryFling runs before the hit is known. -- fixed in 28326b27d
- **low** *Gastro Acid spares only Multitype* -- the games' unsuppressible set applies there too. -- fixed in 361df8069
- **low** *the trainer AI sees Judgment with a Pixie Plate as Normal* -- the three sites that type Judgment for the AI read the plate the old way. -- fixed in 4236a5f56, 18a99f944 (matching) and d407c5c41
- **medium** *Electric types can be paralysed* -- subscript_0031_Paralyze.s has no Electric-type test; its "doesn't affect" block is unreachable after the `GoTo` that ends the block above it. The reference's subscript sends an Electric type there. -- fixed in b48005002
- **medium** *Safeguard still stops a held item's status* -- subscripts 0025 (Flame Orb), 0037 and 0047 (Toxic Orb) test SIDE_CONDITION_SAFEGUARD on the held-item path; the reference comments that out as a Generation V change. -- fixed in 9c5c2aa9d
- **medium** *Protect's odds on a run are Gen 4's* -- sProtectSuccessChance (asm/overlay_12_battle_command.s) halves each time and stops at 1/8; the reference divides by three each time. -- fixed in 5d57ce707 (matching) and 9533136ca
- **low** *Sheer Force and Covert Cloak miss five moves* -- IsSuppressibleSecondaryEffect needs a side-effect flag and a chance above 0, which leaves out Psychic Noise, Anchor Shot, Spirit Shackle, Thousand Waves and Throat Chop; the reference lists them. -- fixed in e7b21ff36
- **low** *Heal Block uses retail's move list* -- the reference's effect list also blocks the draining moves, Dream Eater, Matcha Gotcha, Heal Pulse and Life Dew. -- fixed in a490618c9
- **low** *a substitute does not stop nine imported moves* -- Soak, Magic Powder, Forest's Curse, Trick-or-Treat, Quash, Guard Split, Power Split, Heal Pulse and Decorate: their subscripts have no substitute check, which the reference makes in C before the move. -- fixed in 5b32744ec
- **low** *imported moves missing a piece* -- Surging Strikes can miss its critical hits (effect_script_0368 lacks CRITICAL_STAGE_ALWAYS); Matcha Gotcha does not thaw a frozen user; Mortal Spin only poisons, without Rapid Spin's clearing; Shed Tail makes the decoy and never switches out, and its subscript resets the user's stages; Stuff Cheeks eats its berry at +6 Defense; Coaching does not fail without a partner; Hyperspace Fury does not lift an ally's team guard as Feint now does; Greninja-Ash's Water Shuriken does not always hit three times. -- fixed in 5c8581d86, 443d539d8, 790498bcf, 6caa33c16, 7539469e9, 9c61e8de1 and 6133cddf6; Hyperspace Fury already does what the reference does
- **low** *Comatose reads the reference has and this tree has not* -- Nightmare, the Role Play, Skill Swap, Gastro Acid and Worry Seed block lists, and the sleep bonus to a catch. -- fixed in 9969112f4
- **low** *an Ability Capsule's swap is undone by the next ability reset* -- the capsule writes the ability outright, and UpdateBoxMonAbility, run on evolution and on form changes, works it out again from the personality; the reference keeps a swap bit that its reset honours. The field it uses here already holds the Mint's nature bits. -- fixed in 7bb4ac919
- **low** *the party menu closes after a Potion even with more in the bag* -- the reference's PartyMenu_ItemUseFunc_ReuseItem keeps "Use on which Pokemon?" open after an item that is not an evolution item while more remain. -- fixed in 701ae7403
- **medium** *retail species' level-up learnsets are pret's, not hg-engine's* -- the importer kept pret's learnsets for HeartGold's 493 species apart from konefr's fifteen, while the engine at d0380a487 carries modern ones; by the provenance rule those are the engine layer's values. The visible case: Dunsparce never learns Hyper Drill (hg-engine teaches it at 32), so Dudunsparce, and its passive Three-Segment form, cannot be had. -- fixed in a65853039 (hg-engine's) and ef3b52436 (konefr's fifteen over them); Dunsparce learns Hyper Drill at 32
- **low** *trainer species above 1023* -- trainer_data.c masks a party entry's species with 0x3FF; no trainer holds one today. -- fixed in 166d1b1c0, before the row was written
- **low** *a fresh worktree's first build hangs* -- on a clean checkout make spends half an hour or more re-scanning missing generated files (msg_0729.gmm is an order-only prerequisite of every message file, headers.done of every script); building those targets first, or the ROM target directly, avoids it. -- fixed in af57c1ed5 and 0aa38ff5a

## Found in the fifth round, 2026-09-23

- **medium** *Orichalcum Pulse never turns the sunlight harsh* -- only its Attack boost is written; nothing on entry sets the sun, where the reference has a switch-in step and subscript 487. -- fixed in d4432d1f7
- **medium** *Intimidate ignores the later games' blockers* -- Inner Focus, Oblivious, Own Tempo and Scrappy do not block it and Rattled does not answer it (subscript_0186_Intimidate.s); the reference does not either. -- fixed in 1294f89ef and b32247bd5
- **medium** *Berserk, Anger Shell and Pickpocket ignore Sheer Force* -- their check reads ctx->unk_2174 after command 29 has cleared it. -- fixed in 400565bf9; and three more of Sheer Force's exceptions from Pokemon Central: 84fb2f024, f109afdef and 5ea21921f
- **medium** *Emergency Exit and Wimp Out answer only a move's damage* -- recoil, Life Orb, hazards, weather, status, Leech Seed and Curse do not set them off; Pokemon Central says hazards do. The reference has the same gap. -- fixed in df7e47aa4, d1df82022 and 55f3053d6; the end of a turn is asked once its last effect is over, so a Leftovers heal in between keeps it in; a missed High Jump Kick arms nothing
- **medium** *item-driven forms* -- Genesect's Drive, Silvally's Memory (RKS System reads plates in both trees), Ogerpon's masks and the origin items set no form here. -- fixed in 8a8902879, 38992eaa3, be189899c, cc254f248 and 356311335
- **medium** *items a species keeps* -- the reference's CanItemBeRemovedFromSpecies protects Drives, Memories, masks, the Rusted Sword and Shield, the Griseous Core, Booster Energy and mail from Trick, Knock Off, Fling, Magician and Pickpocket; here only the Griseous Orb and Multitype, and Knock Off still refuses any Multitype holder's item. -- fixed in 1e32790c0
- **medium** *an added species' Dex number is its species id* -- Lillipup prints No. 0508, national 506, and Pecharunt 1041, national 1025 (Pokedex_ConvertToCurrentDexNo); the reference prints national numbers. -- fixed in a73b8aa72
- **low** *three heaps within 1.4 KB of full* -- measured in d69344404: heap 1 (the save) keeps 652 bytes at its fullest, heap 64 408 and heap 69 1,340; any growth there fails. -- closed: the reference enlarges nothing the port has not already enlarged (heap 1 is FULL_SAVE_SIZE under EXPAND_PC_BOXES, heap 3 cut to make room); the margins stand as measured, and the next growth of those heaps is where to look first
- **low** *Solar Blade is not halved outside the sun* -- CalcMoveDamage checks only Solar Beam. -- fixed in 37a5f4132
- **low** *Powder cancels the Fire move before its PP is spent* -- case 17 of ov12_0224B528 runs before the PP is deducted. -- fixed in 13051e6de
- **low** *Weather Ball ignores snow* -- as the reference; the ninth generation makes it an Ice move of 100 power. -- fixed in 5a6db0e3e
- **low** *Weather Ball's type can be left unset* -- GetDynamicMoveType sets it only inside the weather branches, so clear skies, fog, snow, strong winds, Cloud Nine and Air Lock return an uninitialised value that Lightning Rod's redirection reads; the AI copies too. Retail's bug, kept by the matching decompilations. -- fixed in ed1fdef66
- **low** *Minior leaves every battle in its Meteor Form* -- sFormReversion maps Core to Meteor; Pokemon Central says it is always Core outside battle. -- fixed in 3965cc5d4
- **low** *Anticipation in strong winds* -- it reads the same chart the winds change, so it no longer shudders at a move super effective on Flying. -- fixed in fe05cfc9c
- **low** *Oblivious does not stop Taunt* -- neither the subscript nor the immunity check asks it. -- fixed in eefd301e2
- **low** *Psych Up adds Focus Energy instead of copying it* -- BtlCmd_CopyStatStages ORs it in. -- fixed in 38218dbea
- **low** *Poison Touch goes through Safeguard* -- subscript_0022 jumps past the Safeguard check for an ability's poison; to confirm on Pokemon Central. -- fixed in 426d44048
- **low** *the entry abilities are not asked at the end of the turn* -- Opportunist copies an end-of-turn Speed Boost, and Symbiosis replaces a Berry eaten then, only after the next move. -- closed: not a defect as written -- the turn's end reaches the entry abilities before the next choice (2adb1a146 pins the path); what is left is that they act after the last end-of-turn effect, a row of its own
- **low** *As One does not keep foes from eating Berries* -- BerryCanBeEaten asks only for Unnerve, as the reference does. -- fixed in 2f2126fa5
- **low** *Belch's eaten-Berry memory is by battler* -- a Pokemon back in the other slot of a one-trainer double forgets its Berry; the reference keys it as c0465e637 now keys the entry abilities. -- fixed in 3a98b1cf9
- **low** *a sound move's own effects are stopped by a substitute* -- its damage goes through since fb7bed50a, but Growl, Confide, Parting Shot, Sing and Supersonic do not. -- fixed in 0b16b62a2
- **low** *the Ability Shield does not stop the ability-changing moves* -- Gastro Acid, Worry Seed, Simple Beam, Skill Swap, Role Play and Entrainment never ask it (Pokemon Central, Scudo abilita). -- fixed in c3be5398c
- **low** *Cheek Pouch does not heal after a type-resist Berry* -- subscript 264 eats it without the heal the reference's subscript gives. -- fixed in 5f9e416c1
- **low** *a missed Fling still applies a Leppa Berry or a White Herb* -- TryFling writes them before the hit is known. -- fixed in cc4ed3544
- **low** *end-of-turn scripts read a stale attacker* -- Yawn's sleep runs with whoever moved last as attacker, so an Infiltrator that moved last carries it past the sleeper's substitute, as in the reference. -- fixed in 3b14822a7; the row's Yawn and Infiltrator example could not happen: the sleep subscript's move-effect path never reaches CheckSubstitute
- **low** *Floral Healing has no effect* -- it is effect 0 here and MOVE_EFFECT_HIT in the reference. -- fixed in 83b727edb
- **low** *the held items' limits* -- a Gem is not spent on a fixed-damage move; U-turn and its family leave mid-move, so the Eject Button is never asked against them; one switching item acts per move; the Eject Pack does not answer entry drops after a Red Card's replacement; the Mirror Herb sees only ChangeStatStage raises and ignores Contrary; the Binding Band is read when the damage is dealt. -- fixed in 78b4c22b5, 6dbf3e233 and 9e6c3dd62; the Eject Pack after a replacement's entry drops, Mirror Herb's non-command raises and the Binding Band's moment are rows of their own
- **low** *Guard Dog and the Red Card* -- the Red Card drags through TryPickForcedSwitchIn; whether Guard Dog's anchor is asked there is to check. -- fixed in 8df7bbb08
- **low** *Lash Out never doubles* -- the reference's anyStatLoweredThisTurn; statLoweredBattlers here is per action. -- fixed in d438808fb
- **low** *a bind lasts retail's 2-5 turns* -- the reference's 4-5, 7 with Grip Claw. -- fixed in 5b9fc80b3
- **low** *what takes the attacker's slot mid-move is choice-locked into its move* -- only U-turn is excused, not Volt Switch, Flip Turn, Parting Shot or the Red Card. -- fixed in 0eb4387ac
- **low** *a Pokemon dragged in mid-hit answers the hit's on-hit steps* -- unverified beyond the Red Card and Eject Button (c172085f1). -- fixed in 3eb0860cd; the dragged-out Pokemon's own answers are now lost, a row of its own
- **low** *Roar, Whirlwind and Dragon Tail keep Gen 4's level test in trainer battles* -- the reference inherits it; the Red Card skips it. -- fixed in 08ae8e2bc
- **low** *the trainer AI types Techno Blast and Multi-Attack as Normal* -- as it did Judgment. -- fixed in cfdb1277d
- **low** *Emergency Exit against U-turn is decided before the held items* -- a Sitrus Berry healing back above half does not keep the U-turn user in. -- fixed in d483249ad
- **low** *Illusion shows through three asm routines* -- the substitute sprite swap, a faint without a hit and the player's health box after a level-up read the real Pokemon, as in the reference. -- fixed in defd2cb88 and ddb7ab0b5
- **low** *the ability-copy lists are narrower than the games'* -- Trace's is retail's three, Entrainment asks nothing, Wandering Spirit's is short. -- fixed in 617cf2cef, cadbc02b8, 306abc941, cdffe8429 and cf4604a5b; Doodle waits for its move, one of the unimplemented ones
- **low** *a multi-strike move through Sleep Talk strikes once* -- Gen 4's loop; Parental Bond too. -- fixed in 4ba16a80f
- **low** *Parental Bond's leftovers* -- recoil per strike, Spit Up's stockpile and Natural Gift's Berry kept when the first strike faints, Pollen Puff on an ally unchecked. -- fixed in 40c32f5b1, 59431122f and 301797b17
- **low** *Dancer's leftovers* -- a stat dance at +6 is still copied, and a dancer locked into another move stays still. -- fixed in c909d51d3 and 338fbaa2f
- **low** *Trash Cloak Wormadam has no machine learnset* -- SPECIES_500 is missing from the reference's learnsets.json, a reference defect. -- fixed in 6260e4960 and b0a93967b
- **low** *retail forms 496..507 keep Gen IV level-up learnsets* -- their bases have hg-engine's; the reference has none for 496..498 and 500. -- fixed in 0c7b98f0a; the reference has lists for all but 500, the Trash Cloak, whose own came from Pokemon Central
- **low** *the Rotom Catalog overwrites move 1 when Rotom knows four* -- as the reference; the games ask. -- fixed in d83100a55
- **low** *the battle bag's 36-slot lists* -- whether the widened pockets feed more than 36 items into one is not checked. -- fixed in 1b337377b
- **low** *Conversion 2 is Gen 4's* -- it reads the move that last hit the user; the later games read the target's last move, single target. -- fixed in fd54d7cd3
- **low** *Rapid Spin's raise is not a secondary effect* -- Sheer Force neither boosts it nor drops the raise, and the Covert Cloak does not stop it. -- fixed in 0a7613dcd
- **low** *Beat Up is valued at 1 by the strongest-move AI* -- its table power is the engine's 1; msg_0197 rows 1794-1796 are unused since. -- fixed in 898442cd0; msg_0197 rows 1794-1796 stay, unused, since removing them would renumber every row after
- **low** *effects 62, 63 and 64 carry pret's names for each other's scripts* -- nothing reads the names today. -- fixed in ce1f7ef7e
- **low** *the Dex front page's counts print three digits* -- a count past 999 would show '?xx'. -- fixed in 65f2d3165
- **low** *a course record with a truncated species asserts when viewed* -- one saved before bb0a9def3 with a species 494..511 fails ov98_0221EE28's check. -- fixed in bcc423cc7 and ac35e5604
- **low** *removing an asm include stops the next build* -- an asm object's dependency file names its includes with no empty rules, so a deleted .inc (overlay_12_battle_command.inc after 5d57ce707) is a missing target until the .d is removed. -- fixed in 4bc26c74d
- **low** *an archive is not rebuilt when o2narc changes* -- the Dex archives had to be deleted by hand after f9de102b7 to take its fix. -- fixed in 6c022ccdc
- **low** *test_retail_effect_scripts says Parental Bond's commands are stubs* -- they are not since a6ee2c81c; each script listed under PARENTAL_BOND wants its real reason. -- fixed in 40ef777e6

## Found in the sixth round, 2026-09-23

- **high** *every imported item showed another file's icon* -- the icon archive is packed by file name, so item_icon_1000 sorted before item_icon_101 and 343 of the 518 imported icons pointed at another member, some at a palette, which hung the bag on an assertion (the DNA Splicers). -- fixed in ab8abb720
- **medium** *a renamed subscript's old .bin is packed into the archive* -- the archive rule packs every .bin in the folder, so a script renumbered or removed leaves its old .bin in the next build and every script after it runs the wrong code; the integration removes them by hand. A test that member N is subscript_N, and a rule that packs only what a .s makes, are wanted. -- fixed in 2da11f15f; test_build_rules holds member N of each battle script archive to script N
- **medium** *the tests count three abilities done by a list alone* -- test_ability_effects' "read by the game" check takes a block list as a reading, and Commander, Gulp Missile and Poison Puppeteer pass that way; whether each really does what Pokemon Central says is to check. -- open for Commander: Poison Puppeteer (66281a56e) and Gulp Missile (a76cef294) do what Pokemon Central says, and a block list no longer counts as a reading (acc7c51ae), which leaves Commander in PENDING. It does nothing in a double battle yet; it wants a commanding and a commanded state, an entry step that hides Tatsugiri and raises Dondozo's five stats by two, Tatsugiri skipping its choice and missed by every move, neither able to leave or be forced out, and Tatsugiri back when Dondozo faints (Pokemon Central, Torre di Comando)
- **medium** *78 moves the engine leaves unimplemented still do nothing* -- FLAG_UNUSABLE_UNIMPLEMENTED moves keep effect 0 and cannot be learnt or used, Dragon Cheer and Doodle among them; Paolo's rule is that nothing the reference leaves empty stays empty, and import_moves.IMPLEMENTED_HERE records each one done. -- open for six: the seventh round wrote 54 of the 60 that are not Dynamax's (33ce772a8 to f5aa5b4e2, each in IMPLEMENTED_HERE), and the 18 Max moves are Dynamax's, out of scope by SCOPE.md; left are Water, Fire and Grass Pledge (the combination at 150 power, the sea of fire, the swamp and the rainbow, which Court Change would swap too), Ally Switch, Sky Drop and Revival Blessing (a fainted party member picked mid-move, by the player and by the AI), each new controller code in overlay 12
- **medium** *the weather abilities set permanent weather* -- Drought, Drizzle and Sand Stream still set retail's permanent weather; the games give five turns, eight with the matching rock, as Snow Warning and Orichalcum Pulse do here. -- fixed in dd9ca0b22, 50bf176be and 7b7aa039a; Sand Spit's sandstorm too, in 7cfd90480 and 7547dfef1
- **medium** *Defog is Gen 4's* -- it clears only the target's side and leaves Sticky Web and Aurora Veil; the engine's subscript 171 clears both sides but takes the hazards off the wrong side's queue, not to copy. -- fixed in 2c4affe08
- **medium** *Tidy Up never clears Stealth Rock or Sticky Web* -- subscript 326 tests battler ids where it means side conditions, as the reference's 445 does. -- fixed in 087370d84
- **medium** *the continue screen prints the Dex count with three digits* -- "POKéDEX ?25" (ov74_02227E64, formatted for the play time's hours); the trainer card is to check too. -- fixed in 585e86ba7, 2977f24ce (matching) and a3b2048af, the save window, the trainer card and Oak's Dex call with it; seen as POKéDEX 1025 on all three screens
- **medium** *four Gen 9 icons are garbled* -- Walking Wake, Iron Leaves, Munkidori and Iron Crown (1009, 1010, 1015, 1023) show as stripes in the Dex grid, the party and the PC. -- fixed in 6c59633e0 and ee3154572: the species check found sixteen striped icons, not four, now built 4bpp whatever their PNG's depth, and 21 added icons drawn in a palette not theirs; 1009, 1010, 1015 and 1023 seen as pictures in the Dex grid
- **medium** *Kingambit and Gholdengo cannot be had* -- the methods work, but no Bisharp holds a Leader's Crest and no Gimmighoul Coin is placed anywhere; konefr places neither. -- open: Paolo's design (2026-09-23), beyond konefr's, is Bisharp evolving by levelling up knowing Swords Dance, and Gimmighoul, both forms, learning Pay Day at 55 and evolving by levelling up knowing it
- **medium** *Milcery cannot evolve* -- EVO_SPIN_* is never checked in the reference, and its nine Alcremie rows point at forms 1..8, where 7 is Gigantamax and 8 does not exist; the adaptation (which cream from which spin, and forms that exist) wants a decision. -- open: Paolo's design (2026-09-23) is levelling up anywhere in the Ice Path holding a Berry, which picks the Sweet and is used up (Cheri Strawberry, Oran Berry, Pecha Love, Sitrus Star, Lum Clover, Aspear Flower, Nanab Ribbon), and no other way; the reference's spin rows are not copied
- **low** *Weather Ball ignores the Utility Umbrella* -- Pokemon Central (Palla Clima): the holder's Weather Ball keeps rain's and sun's type and power; the reference does not ask either. -- fixed in 6052bc878
- **low** *the trainer AI types Weather Ball from the field* -- ov10_0221F47C and ov12_02258BB4 read fieldCondition rather than BattlerMoveWeather, so they miss Mega Sol. -- fixed in 84fb7d22d; its damage estimate leaving Weather Ball untyped is a row of its own
- **low** *a Minior follows as the red Meteor Form* -- the Core Forms have no follower sprite of their own, and a Minior is always Core outside battle now. -- open: 3350e975b writes down why. Neither tree has a follower picture of a Core: the reference's seven are hg-engine's placeholder, Bulbasaur's, so the Core Forms walk as the Meteor model. It closes with art, one 32x256 eight-frame follower of the Core with seven palettes, mapped to the seven SPECIES_MINIOR_CORE_* by import_followers.py and follow_mon.c; the six Meteor-colour models (1237-1242) are Minior's picture and palette again and never shown, the slots to reuse
- **low** *a bounced Parting Shot switches nobody out* -- Magic Coat and Magic Bounce should switch the bouncer (Pokemon Central, the reference's own battle test). -- fixed in 9c28e240b
- **low** *Parting Shot on a Contrary target* -- the target's stats rise and the user stays; in the games any stat change sends the user out. -- fixed in 987069f00
- **low** *make's built-in rules are live* -- no -r and no empty .SUFFIXES: a stray file once had make try to remake a .d with Pascal's compiler. -- fixed in aca16d493, an empty .SUFFIXES rather than -r, which would reach the sub-make that builds nitroarc
- **low** *import_moves.py --unimplemented is dead* -- it rewrites a table in src/move.c that no longer exists. -- fixed in ae981d5e6
- **low** *type changes keep an added third type* -- Soak, Magic Powder, Camouflage and Conversion set only two; Conversion is Gen 4's, a random move's type. -- fixed in c772ec596; and Conversion takes the type of the user's first move, as from Gen VI, in 074510661
- **low** *a Covert Cloak stops the attacker's own additional effects* -- Power-Up Punch, Flame Charge, Rapid Spin's rise; as the reference's list; the games leave the user's alone (to confirm). -- fixed in 972cd6efc, confirmed on Pokemon Central (Anonimanto); Rapid Spin's clearing stops at a cloak or Shield Dust on the target, as in Gen IX (Rapigiro)
- **low** *Effect Spore after Parental Bond's first strike* -- the move ends after checks that expected a second strike; the recoil and the held-back effects are lost. -- fixed in 23d238f7d; a second strike that misses is a row of its own
- **low** *Plant Wormadam learns the Trash Cloak's moves* -- the reference's per-game data keys the Trash Cloak's moves under SPECIES_WORMADAM; Pokemon Central gives the Plant Cloak none of them. -- fixed in f291e3f65
- **low** *Payback doubles against a Pokemon that switched in* -- Gen 4's rule; Pokemon Central drops it from Gen V (to confirm). -- fixed in af4cd217e, confirmed on Pokemon Central (Rivincita)
- **low** *Fissure does not reach a Pokemon underground* -- the reference lets Fissure and Magnitude reach Dig (to confirm). -- fixed in 2b9f3ec01, confirmed on Pokemon Central (Abisso)
- **low** *Worry Seed, Simple Beam and Gastro Acid keep their own lists* -- the ability table the copiers ask now refuses Zero to Hero and Tera Shift, which they miss. -- fixed in f85e7748a; Core Enforcer asks the same table since fd0b3bdb3
- **low** *Sheer Force's last exceptions* -- a boosted Relic Song changing Meloetta's form, and Shell Trap (Pokemon Central, Forzabruta), are unchecked. -- fixed in 65e7ba2d4 (Relic Song); Shell Trap asks it from its own commit, 0f022471a
- **low** *Opportunist and Symbiosis act late at the end of a turn* -- after the last end-of-turn effect rather than at once. -- fixed in b78b644c1
- **low** *a dragged-out Pokemon's answers to the hit are lost* -- Dragon Tail, Circle Throw and Roar drag before the on-hit steps, so its Rocky Helmet, Rough Skin or Justified never go off. -- fixed in 9db5f7b28 and c732334ac: Dragon Tail and Circle Throw drag in the step for the move's additional effects, once the hit is answered and only if the user still stands; Roar and Whirlwind deal no damage, so nothing answers them
- **low** *Belch credits a plucked or flung Berry to the wrong Pokemon* -- the eater should be told (TryEatOpponentBerry, FlungItemLands). -- fixed in b165eae8d
- **low** *the Ability Shield does not stop Mold Breaker and its kind* -- Pokemon Central (Scudo abilita). -- fixed in d2a3fcae9, after b3e095ed7
- **low** *Cheek Pouch's pending heal ignores Heal Block* -- the reference's subscript 458 refuses (to confirm). -- fixed in d47330fd3, confirmed by the reference's subscript 458 and Pokemon Central (Guancegonfie)
- **low** *a Red Card holder's own U-turn* -- Pokemon Central (Cartelrosso): U-turn, Volt Switch and Flip Turn do not switch a user holding the card (to check). -- fixed in 9c5c98f1e, a reference defect (Pokemon Central, Cartelrosso); 94f9bde88 reads the card as the user would go
- **low** *the Eject Pack after a replacement's entry* -- it is not asked for drops on a switch-in after a Red Card, an Eject Button, Intimidate or Sticky Web. -- fixed in 21d4c5c71 and b9cd07473
- **low** *the Mirror Herb sees only the stat command's raises* -- Belly Drum, a Starf Berry, Stuff Cheeks go unseen. -- fixed in d9eb30c12, and in 51906ccee not for an Opportunist's copy (Pokemon Central, Foglia carbone)
- **low** *the Binding Band is read when the damage is dealt* -- not when the bind starts. -- fixed in 2faf3c629
- **low** *Future Sight at landing does not ask Disguise, Ice Face or Tera Shell* -- fixed in bbda1a90a; Ice Face guards only against physical moves, so it has nothing to do there
- **low** *Silvally is typed by its Memory in the summary* -- Pokemon Central (Sistema Primevo) says the ability has no effect outside battle (to confirm). -- closed: not a defect; the games type a Silvally by its Memory outside battle too (Bulbapedia's Memory pages), as retail types Arceus by its plate, and the RKS System page speaks of the ability, not the form; its picture is a row of its own
- **low** *every added species has body style 0* -- the reference's Species.c leaves the placeholder for all past Arceus; the body-style search lists only HeartGold's 493. -- fixed in 8ae70cd5b, from Bulbapedia's list by shape (revision 4616890), forms taking their base's; Sneasler's is a row of its own
- **low** *Giratina Origin's Dex figures are Altered's* -- one set of values replaced the retail pair in zukan_data.json. -- fixed in 360aca7ae
- **low** *Galarian Slowpoke and Slowbro are Dex species of their own* -- no sort list can hold them, yet they are counted when caught. -- fixed in 2d01acac8: they count as Slowpoke and Slowbro, as hg-engine counts its forms
- **low** *the Dex grid's last page* -- at 1025 it ends in two empty rows the cursor can rest on. -- fixed in fc526e966: the cursor stops on the last species
- **low** *the Dex hides a Normal second type* -- Litleo and Pyroar show one icon; the reference lifts that check in the Dex and the PC. -- fixed in 973888dc9 (matching) and a89cc79c3, in the Dex and the PC; the species check found 22 species and forms showing one type
- **low** *the Dex's type search has no Fairy* -- pure Fairy species cannot be found by type. -- fixed in 5e05ac20b, a8b08efcd (matching), ff4204d23 and 4c4f631ee (New Gold's Fairy types, Meganium and Mismagius)
- **low** *GTS trades never evolve Karrablast or Shelmet* -- ov70_02241648 (asm) passes no partner. -- fixed in ffbc87110 (matching) and 3e8629c64
- **low** *the critical-hit mark outlives a cancelled evolution* -- a Galarian Farfetch'd cancelled or fainted evolves at a later level-up; fleeing should not count. -- fixed in 95d685e26: the mark lasts the battle; that a fled battle does not count rests on this row alone, a row of its own
- **low** *EVO_MOSSY_ROCK and EVO_ICY_ROCK are not ported* -- neither tree's data uses them yet. -- fixed in 196be0087
- **low** *the party menu shows the old panels after the DNA Splicers* -- until it closes, right after the scene. -- fixed in 57df6e84e
- **low** *a save has no layout version* -- Save_GetLegacySlotSpecs knows the one layout before the DNA Splicers' block; the next change to the save needs a version, and savedit and saveui have to read both. -- closed in e91907b3b: no version yet, since the footer magic counts as version 0 and the two layouts are told apart by the first slot's size; docs/newgold/SAVE-LAYOUT.md says what the next change must do, and test_save_legacy pins both layouts' sizes

## Found in the seventh round, 2026-09-24

What the seventh round's nine branches, their merge, the boot arena's budget
and the landing on newgold found. "To confirm" waits on Pokemon Central or a
second look; "decision" on Paolo.

- **high** *battles with a Surge user hang* -- Tapu Koko, Lele, Bulu and Fini and Mega Raichu X announce their terrain for ever, on either side: BtlCmd_AbilityPopup cleared abilityActivatedFlag, the Surges' only once-guard, which the reference's command leaves alone. Found by the species check. -- fixed in 6482b58d3
- **high** *the boot arena ran out* -- the main arena starts where overlay 12 ends, and the round's moves moved it 0x1460 on: the file system's table (0x1B80) no longer fitted and all three ROMs stayed on a white screen, the diagnostics build from the eight branches on. The heaps take the arena at boot, and test_heaps measured only heap 3's own room. -- fixed in 24d3d77c5 (the default heap 0xD200 to 0x8000 in both builds; its worst measured use is the communication-error screen's 0x5950) and f6c1b9364 (test_heaps holds each built ROM's arena to the byte); 0x4408 left at the worst pre-size, 0x3008 in the diagnostics build
- **medium** *18 added cries never played* -- Samurott, Guzzlord, Coalossal, Eternatus, Glastrier, Calyrex, Maushold, Arboliva, Garganacl, Klawf, Espathra, Bombirdier, Orthworm, Cetitan, Dondozo, Baxcalibur, Fezandipiti, Archaludon and the forms that share them: bank and wave archive were over the cry player's 24,200-byte heap. -- fixed in 1f5eecc29, the 18 resampled at 7.2-10.4 kHz; ADPCM, as the reference, if the lower rates are heard
- **medium** *savedit's Dex set seen and caught only* -- never the genders seen or the form orders, so an edited save asserted in the Dex on a female-only species and showed Sky Shaymin, Origin Giratina and East Sea Shellos as first forms; saves edited before keep the bad bits. -- fixed in 00172ca04, on the save editor's rewrite
- **medium** *added species' Dex entries overflow their window* -- 87 lines longer than retail's longest, twelve Gen 9 entries with no break, Raging Bolt's and Pecharunt's cut to one line. -- fixed on newgold in c8192a543, 090647c57 and 43af5b307 (broken again to fit, measured with font 0), and for the 116 no breaking fits, two pages that turn by themselves (Paolo, 2026-09-24) in df017fe45, 2f570f9e9, d0ed3cb71 and cc2c01c5b; Iron Crown's seen turning every 300 frames on the landed build
- **low** *the summary's cry asked for the ability as the form* -- 82722b7ba swapped the record's form and ability and missed this reader, as the reference does; Sky Shaymin played the Land Forme cry. -- fixed in 0d6e34d71 (matching) and b07546e08
- **low** *the Dex search's RESULTS count printed three digits* -- 'RESULTS ?25'. -- fixed in 182c7d726 (matching) and 6f25dac0d; seen as 'RESULTS 1025'
- **low** *a stage a script adds to could pass +6* -- Steam Engine's subscript 365 pushed Speed past it: UpdateMonData did not clamp the stages. -- fixed in d1e6f7cdb
- **low** *Sticky Hold kept the item of a holder the move felled* -- fixed in a76a274ae
- **low** *Fling refused every Multitype holder and the Griseous Orb* -- fixed in 4ea52a01a
- **low** *Fling worked for a Klutz holder* -- fixed in 23f626c71
- **low** *Worry Seed failed on a Pokemon holding the Griseous Orb* -- retail's check, which Pokemon Central drops from Gen V and the reference's script does not have. -- fixed in e5adc4e56
- **low** *Teravolt and Turboblaze did not pass Lightning Rod or Flower Gift by* -- the redirection, the defending side's Flower Gift and the AI's Levitate and Wonder Guard check asked for Mold Breaker by name. -- fixed in b3e095ed7
- **low** *gym.py stopped silently on a two-word species* -- it took the battler line's fourth word for the HP, 'L30' after "Raichu Alolan" or "Iron Crown", and sent stderr, the traceback with it, to /dev/null. -- fixed in 017ef7d2a
- **low** *test_boot near the margin depends on the clock* -- the pre-size comes from the harness's clock second, so a build 0x20 over the margin drew the title in 3 boots of 8. -- closed: test_heaps is the guard; it fails such a build on every run, to the byte (tried at 0x4400 and 0x4420 of growth)
- **medium** *Life Dew heals only its user* -- Pokemon Central: the user and its ally, a quarter each; its script compares HP against battle var 48. -- open
- **medium** *five moves use the ordinary stats* -- Foul Play (the target's Attack), Body Press (the user's Defense), Psyshock, Psystrike and Secret Sword (the target's Defense) go through CalcMoveDamage as any move; the reference's CalcBaseDamage handles them. -- open
- **medium** *Photon Geyser neither ignores the ability nor goes physical* -- and the ability-ignoring path Sunsteel Strike and Moongeist Beam take lacks the reference's two guards, an ability that can be broken and attacker != defender. -- open
- **medium** *post-move effects past effect 276 still come with the hit* -- Stone Axe, Ceaseless Edge, Mortal Spin, Jaw Lock, Anchor Shot, Spirit Shackle, Fell Stinger and Scale Shot, which the engine gives after the move; they want the post-move pass the binding moves took, and ov12_02250490's Parental Bond hold-back list. -- open
- **medium** *a catch with a nickname asserts when the party is full* -- the caught Pokemon nicknamed and sent to Box 30 fails CopyWindowToVram+0x14 under RunTextPrinter; seen in the harness on the diagnostics build, where the comm system is up, as the communication-error screen. To reproduce on melonDS. -- open
- **low** *Chloroblast's recoil* -- taken only when it hits, and Rock Head stops it; Pokemon Central: half the user's maximum HP, hit or miss, whatever the ability. -- open
- **low** *Throat Chop silences on a miss* -- the silence is set in the effect script before the accuracy check. -- open
- **low** *the Fangs' status and flinch are on-hit side effects* -- so Sheer Force neither drops nor boosts them and the Covert Cloak does not stop them. -- open
- **low** *Magnet Rise's Ground immunity ignores Gravity* -- in CalcTypeEffectiveness. -- open
- **low** *Stuff Cheeks eats the Berry only when its own condition holds* -- it should eat any held Berry, then raise Defense by two. -- open
- **low** *Burn Up and Double Shock leave a Normal type* -- RemoveBattlerType writes TYPE_NORMAL where its comment says typeless; the reference's RemoveType writes TYPE_TYPELESS. As Normal it is immune to Ghost and weak to Fighting. -- open
- **low** *Order Up raises nothing without Commander* -- its raise depends on the Commander row. -- open
- **low** *Gems can be flung* -- item_data.csv gives them a fling power of 30; the engine's Fling refuses them. To confirm. -- open
- **low** *the post-move order after the Life Orb is not the engine's* -- here Parting Shot, Pickpocket, Throat Spray and the Eject Pack come before Emergency Exit, Wimp Out and U-turn; the engine asks Emergency Exit (22.0), then U-turn and Parting Shot (23.0), Pickpocket (24.0), Throat Spray (26.0), the Eject Pack (28.0). -- open
- **low** *Magician and Pickpocket act in wild battles* -- Pokemon Central (Prestigiatore, Arraffalesto) says they do not; CanStealHeldItem has no check. -- open
- **low** *Pickpocket takes nothing after a Red Card* -- the attacker is already dragged out; Pokemon Central (Arraffalesto) says the holder still steals. -- open
- **low** *Knock Off's line comes after "fainted"* -- the post-move pass runs after the faint; Pokemon Central (Privazione) removes the item first. Message order only. -- open
- **low** *Dragon Tail's target keeps quiet before a Rocky Helmet fells the user* -- Battler_WillBeDraggedOut is asked when the hit is answered, so its Pickpocket, Color Change and Anger Shell are skipped though it stays in (a ponytail comment in overlay_12_0224E4FC.c). -- open
- **low** *Conversion, Soak and Magic Powder refuse only Multitype* -- the reference's Soak and Magic Powder refuse RKS System too. -- open
- **low** *a Parental Bond second strike that misses loses what the first held back* -- MOVE_STATUS_MULTI_HIT_DISRUPTED after the loop started; whether this game rolls the second strike's accuracy is to confirm. -- open
- **low** *the ability-changing moves against Commander, Protosynthesis and Zen Mode* -- Pokemon Central's pages disagree (Torre di Comando, Paleoattivazione, Gastroacido, Affannoseme, Stato Zen); the port follows the table. Decision. -- open
- **low** *the trainer AI's damage estimate leaves Weather Ball untyped* -- ov10_0221F084 gives it no weather type or doubled power, retail's gap. -- open
- **low** *a transformed Cramorant neither catches nor spits* -- Pokemon Central says only that a copy cannot spit; changing form here would write the copier's party entry. To confirm. -- open
- **low** *Sand Spit writes over the map's weather* -- as Pokemon Central (Sputasabbia) gives no exception; the reference goes through Sand Stream's subscript and refuses. To confirm. -- open
- **low** *two Surge users entering together both announce the terrain* -- the comment calls it the reference's behaviour. To confirm. -- open
- **low** *Burning Jealousy and Alluring Voice see only the stat command's raises* -- statRaised is set by ChangeStatStage alone; a raise a script writes (Belly Drum, Anger Point, Steam Engine) is not seen. -- open
- **low** *BtlCmd_TriggerAbilityOnHit is unused* -- no script runs it since subscript 175 took the engine's shape; it is still in the command table. -- open
- **low** *Silvally's picture ignores its Memory* -- the type follows the Memory, the picture does not: no Silvally case in the pokegra switch and no form species. Whether the follower follows it is to check. -- open
- **low** *the critical-hit mark may outlive a link battle* -- the link end path never reaches BattleSystem_CheckEvolution, which clears it; whether that path copies the party back is unverified. -- open
- **low** *species with the reference's placeholder picture* -- Iron Crown's Dex picture is Bulbasaur's, seen at the landing: hg-engine ships Bulbasaur's picture for it and 86 other sprite folders (six of them on the female picture only: Mega Magearna, Mega Meowstic's female and the four Ogerpon Terastal masks). None of them can be met in New Gold as ported; art from outside the reference is a decision. -- open
- **low** *a battle script archive is not rebuilt when only a script is deleted* -- the archive depends on the .bin files, so deleting the last .s makes nothing newer; a .narcorder rewritten only when it changes, as a prerequisite, would. -- open
- **low** *field scripts pack their whole folder* -- scr_seq.narc is still built by the generic rule, so a removed or renumbered script leaves its old .bin and moves every event after it. -- open
- **low** *the .narcorder sorts by name* -- a script past 9999, or one without zero padding, would sort out of number order; test_build_rules would catch it. -- open
- **low** *the harness sometimes draws white for a whole run* -- 3 runs of about 10 with the same input; test_boot's diagnostics boot failed once in the suite and passed rerun. -- open
- **low** *import_dex_metrics.py reports 'updated 729' on a rerun* -- the body styles it puts back; the json does not change. -- open
- **low** *the arena test has gaps* -- test_heaps skips itself when nothing is built and never checks build/soulsilver.us.diag. -- open
- **low** *heap 0 at 0x8000 is not measured everywhere* -- the Pokeathlon (estimated 0x6000), link and Wi-Fi scenes, and an error screen raised inside a Wi-Fi app, were not reached. -- open
- **low** *other screens that draw a Pokemon's types* -- the Normal-second-type check was not looked for in the battle's own Pokemon pages. -- open
- **low** *the species check's blind spots* -- the Dex's FORMS page was not walked, and the summary's Info page was looked at by hand, Litleo and Girafarig. -- open
- **low** *Sneasler's body style* -- Bulbapedia says tailless, PokeAPI and Pokemon Central tailed; the port keeps Bulbapedia's. To confirm. -- open
- **info** *"KYUREM changed Forme!" is shown before the panels change* -- the partner leaves once the line is dismissed, by design since 36a24a778. Decision. -- open
- **info** *a fled battle does not count for the critical-hit evolution* -- it rests on this audit's row, not on Pokemon Central, which asks three critical hits in one battle and 1 HP at its end. Decision. -- open

## Species past retail's tables

### archives

- `src/pokemon.c:2441` files/pbr/pokegra.narc (NARC 194) has 2964 members, which is 494 x 6, so it covers species 0..493 only. sub_02070560 is the default case of DP_GetMonSpriteCharAndPlttNarcIdsEx and has no bound. The working tree guards only the height half... -- fixed in f5a38ddf8
- `src/field/scrcmd_move_tutor.c:156` waza_oshie.bin is 11480 bytes, which is 1435 records of 8 bytes. It leaves out the egg and the bad egg, so species s >= 496 sits at record s-3 (waza_oshie.json[505] is SPECIES_LILLIPUP). The working-tree fix returns species-1 for every... -- fixed in 9eca47d8b
- `src/pokemon.c:4381` files/a/1/8/0 (NARC 180) has one member of 43966 bytes, which is 494 records of 89 bytes (species 0..493). The working tree clamps only NARC_ReadPokepicAnimScript (PokepicAnimSpecies). Its siblings sub_020729D8 (4381), sub_020729FC (4387)... -- fixed in cd825c299 and 5814f49f7
- `asm/overlay_96.s:1039` The Pokeathlon course reads performance.narc (NARC 169, 554 members) through its own species->member table ov96_0221AAE8. That table is 0x3DC bytes, which is 494 halfwords (species 0..493). The working tree guards only the C copy in... -- fixed in 05b6b722e (matching) and d3bd78928: past retail's species the course reads member 0, as pokemon.c's copy does
- `src/pokemon.c:4375` Same archive a/1/8/0 (494 x 89-byte records). sub_020729A4 here and sub_0207294C (line 4365, a3 is the species) are unguarded, although their sibling NARC_ReadPokepicAnimScript was clamped to record 0. -- fixed in cd825c299 and 5814f49f7
- `asm/overlay_112.s:7119` The Pokewalker app loads files/a/2/4/8 (NARC 250, 540 members) at icon index - 7 (ov112_021E9464 here, ov112_021E9470 at 7130). GetMonIconNaixEx now puts the added species at icon species-508+551, which is past everything this archive... -- out of scope (Paolo, 2026-09-23): infrared only
- `asm/overlay_112.s:7296` ov112_021E9480 maps a species to a member of files/a/2/5/6 (NARC 258, 664 members) through ov112_021F397E. That table runs 0x021F397E..0x021F4138, which is 494 pairs of halfwords (species 0..493), and nothing bounds the species. -- out of scope (Paolo, 2026-09-23): infrared only

### asm

- `asm/overlay_96.s:1037` The Pokeathlon course has its own species-to-performance-record table. It is still in asm and has no bound. ov96_0221AAE8 (asm/overlay_96.s:107517) holds 494 u16 entries, for species 0..493. The member it gives is read from... -- fixed in 05b6b722e (matching) and d3bd78928
- `src/pokeathlon/pokeathlon_save.c:88` Pokeathlon medals are stored as one byte per species, in unk_DC[0x1F0] (496 bytes). ov96_021E8028 writes a medal bit for each of the three entrants, indexed by species-1, with no upper bound. The readers are not bounded either:... -- closed (Paolo, 2026-09-23): added species do not earn Pokeathlon medals for now; a medal won by one is not recorded
- `src/pokemon.c:2441` The dress-up portrait (overlay_41, ov41_02249604) draws the chosen mon through sub_02070130. That goes GetBoxmonSpriteCharAndPlttNarcIds(..., TRUE), then DP_GetMonSpriteCharAndPlttNarcIdsEx, whose default case is sub_02070560. That reads... -- fixed in f5a38ddf8
- `asm/overlay_96.s:3244` When the player sets a new event best, ov96_021E786C saves each entrant's species in a 9-bit field of the Pokeathlon event record. The record viewer extracts it with `lsl r0, r0, #0x17; lsr r0, r0, #0x17` (asm/overlay_99.s:457-458) and... -- fixed in 05cd9d56b (matching) and bb0a9def3: bounded, not widened -- a record keeps only species it can show, and added species earn no medals (Paolo, 2026-09-23)
- `asm/overlay_112.s:7292` The Pokéwalker send path has a species-to-walker-sprite table with no bound. In ov112_021E9480, the default case indexes ov112_021F397E (asm/overlay_112_021F35A4.s:223), which is 1978 bytes: 494 entries of 2 u16. The result is passed to... -- out of scope (Paolo, 2026-09-23): infrared only
- `asm/overlay_98.s:1409` The Pokeathlon medal totals only go through species 1..493. ov98_0221F024 counts species with all 5 medals, and ov99_021E6FA0 (asm/overlay_99.s:2819, same `ldr r7, =0x1ED` loop) counts all medals. Both are called at asm/overlay_99.s:2881... -- closed (Paolo, 2026-09-23): added species do not earn Pokeathlon medals for now; a medal won by one is not recorded
- `asm/unk_0202FBCC.s:1775` The battle-video header getter sub_0203088C, case 0, returns species 0 for any of its 12 party species above 493. -- fixed in 0f7acc582 (matching) and 13e3a1443
- `asm/overlay_109.s:4064` The photo album counts the Pokémon in a photo as species 1..493 only. The caller at asm/overlay_109.s:3390-3393 uses the count to pick the caption string ([r5+0x4c] when the count is above 1, otherwise [r5+0x48]). -- fixed in fb09d77ac (matching) and b9151999f

### c-arrays

- `src/pokedex.c:81` The Deoxys form history is still kept in a fixed retail slot: bits 24..31 of word 15 of caughtSpecies and seenSpecies. That slot sat free above species 493, but it is now the seen/caught bit for species 505..512, which includes... -- fixed in bd2b1d237
- `include/application/pokedex/pokedex_internal.h:209` The Dex list inside PokedexAppData was sized to the retail count, and asm/overlay_18.s still hardcodes that count. Commit ea74ddf71 raised NATIONAL_DEX_COUNT from 493 to 1041 and grew this array by 548*4 = 0x890 bytes. Every C field from... -- fixed in 7af85ec9f
- `include/pokeathlon/pokeathlon_save.h:46` The Pokeathlon medal table in the save has one byte per species, 0x1F0 = 496 entries, indexed by species-1. PokeathlonSave_SetUnkDC_AtIndex and GetUnkDC_AtIndex check only the lower bound. Every species from 497 to 1437 reads or writes... -- closed (Paolo, 2026-09-23): added species do not earn Pokeathlon medals for now; a medal won by one is not recorded
- `src/field/scrcmd_move_tutor.c:199` For every added species the tutor index is off by two. The file drops the egg and the bad egg, so the record for species s >= 496 is at s-3, but GetMoveTutorLearnsetIndex returns s-1. Species 1436 and 1437 index past the end of... -- fixed in 9eca47d8b
- `src/pokemon.c:4381` The per-species picture record table (a/1/8/0 member 0) was guarded only in NARC_ReadPokepicAnimScript through PokepicAnimSpecies (:4356). Its five sibling readers, which take the same member and the same species index, are unguarded, so... -- fixed in cd825c299 and 5814f49f7
- `src/pokedex.c:578` The three Johto count loops now run i up to NATIONAL_DEX_COUNT (1041), but LoadSpeciesToJohtoDexNoLUT (src/pokemon.c:3428) returns retail's johtozukan member: 988 bytes, 494 halfwords, species 0..493. Every seen or caught species 494..1041... -- fixed in a178bb080
- `src/pokemon.c:2441` The other game's (pbr) picture archive has retail's six members per species, 2964 = 494*6. DP_GetMonSpriteCharAndPlttNarcIdsEx's default case sends every species here unguarded. This is the sprite sibling of... -- fixed in f5a38ddf8

### guards

- `include/application/pokedex/pokedex_internal.h:209` The Pokedex app struct is sized with NATIONAL_DEX_COUNT (now 1041, was 493), so every PokedexAppData field after 0x878 sits 0x890 bytes later in C than in overlay_18 asm, which still uses retail literal offsets. -- fixed in 7af85ec9f
- `src/pokedex.c:77` Deoxys's form order is kept in bits 24..31 of word 15 of caughtSpecies and seenSpecies. With NATIONAL_DEX_COUNT = 1041 (NUM_DEX_FLAG_WORDS = 33, include/pokedex.h:19) those bits are the real dex flags of species 505..512, so Lillipup... -- fixed in bd2b1d237
- `asm/unk_02030A98.s:1381` Battle Hall keeps one u16 streak record per species in three tables of 0x1EF (495) entries, at +4, +0x3E2 and +0x7C0 of an extra save chunk of 0xBA0 bytes (sub_020312A4). The writer indexes by species with no bound, and the script readers... -- fixed in c66e8178e (matching) and 03246adc9
- `src/pokeathlon/pokeathlon_save.c:88` The Pokeathlon medal bytes are u8 unk_DC[0x1F0], one per species. The setter only checks the lower bound, so an added species ORs medal bits into whatever save data follows. -- closed (Paolo, 2026-09-23): added species do not earn Pokeathlon medals for now; a medal won by one is not recorded
- `src/pokedex.c:577` The Johto LUT is retail poketool/johtozukan.narc: 988 bytes, 494 halfwords for species 0..493, the same limit SpeciesToJohtoDexNo now guards. These loops index it up to 1041. -- fixed in a178bb080
- `src/field/scrcmd_move_tutor.c:156` The uncommitted fix's comment says both species and forms are 'minus the two missing', but only the retail form cases subtract 2. Ordinary added species get index species-1 when waza_oshie.bin stores them at species-3. -- fixed in 9eca47d8b
- `asm/overlay_98.s:1103` Pokeathlon course records store each record team's species in a 9-bit field (ov96_021E786C, `& 0x1FF`). The course-record screen reads it back (ov99_021E5C4C, `lsl #0x17 / lsr #0x17`) and prints it through ov98_0221EE28, which asserts... -- fixed in 05cd9d56b (matching) and bb0a9def3

### importers

- `src/field/scrcmd_move_tutor.c:199` The move tutor reads the wrong record for every added species 508..1437. The record is two places off, and for 1436 and 1437 the read goes past the end of the file. -- fixed in 9eca47d8b
- `files/msgdata/msg/msg_0803.gmm:2296` Galarian Slowpoke (573) and Galarian Slowbro (574) show an empty Dex entry, "????.? lbs." and "???’??”". The real text is in the banks, but at positions the game never reads. -- fixed in fed164417
- `tools/newgold/import/import_dex_text.py:165` import_dex_text.py is not idempotent. Every run appends another 398 rows (573, 574 and 1042..1437) to each of the 27 banks. -- fixed in fed164417
- `tools/newgold/import/import_evolutions.py:170` Eelektrik (657), Charjabug (766) and Tadbulb (959) have no evolution entry at all, because the reference's ITEM_THUNDER_STONE is never mapped to this tree's ITEM_THUNDERSTONE. -- fixed in 419489cfe
- `tools/newgold/import/import_species.py:304` 324 of the 396 forms can learn no TM or HM (empty tms and hms in personal.json). 191 of them are not megas, Gigantamax or primals. The reference gives each of them its base species' machine moves. -- fixed in 419489cfe
- `src/pokemon.c:3149` No importer extends poketool/personal/pms.narc, the baby-species table. It is still retail's 1016 bytes (508 halfwords for species 0..507), so GetEggSpecies returns SPECIES_NONE for all 930 species 508..1437. -- fixed in 9611f21bc
- `src/pokemon.c:4375` Only NARC_ReadPokepicAnimScript was given the PokepicAnimSpecies clamp. Its five siblings read the same retail table without it: sub_0207294C (4365), sub_020729A4 (4375), sub_020729D8 (4381), sub_020729FC (4387) and sub_02072A20 (4393). -- fixed in cd825c299 and 5814f49f7
- `files/application/zukanlist/zkn_data/zukan_data.json:20138` zukan_data.json disagrees with itself. mon_stats has 1438 records, but every sort list (national, alphabetical, heaviest, lightest, tallest, shortest) has exactly 493 species, and the Dex list is built only from those lists. None of... -- fixed in e8ddbbf72 and 5bdb0ccda

## Differences from konefr's reference

### abilities

- **high** *Comatose / Purifying Salt / Flower Veil / Pastel Veil / Water Bubble / Thermal...* -- Six abilities the test lists as IMPLEMENTED have no status-immunity half here. A Comatose Komala can be burned, poisoned, paralysed, frozen or put to sleep, and Hex does not double against it. Purifying Salt and grass-type Flower Veil... -- fixed in ec42c85dc, 89048363c, 0249a9c45 and b4da34137
- **high** *Prankster Dark-type immunity* -- A Prankster-boosted status move aimed at an opposing Dark type lands here. In the reference it "doesn't affect" the target. Only the priority half of Prankster is ported. -- fixed in 93831a0b8 (the priority half) and 550b1de93 (the Dark-type half, which 93831a0b8 had not done)
- **medium** *Queenly Majesty / Dazzling / Armor Tail: which priority they block* -- Here only a move-table priority above 0 is blocked. The reference blocks whatever adjusted priority the turn order used, which includes Prankster +1, Gale Wings +1, Triage +3 and Grassy Glide in grass. Because the reference stores it as a... -- fixed in e96373647: a priority above 0 after the turn order's adjustments is refused, canonically; the reference also refuses negative priority because it reads the value unsigned, a defect not copied (Paolo, 2026-09-23: reference defects in ability code are fixed)
- **medium** *Tinted Lens multiplier* -- In konefr's game Tinted Lens multiplies resisted damage by 1.25. Here it doubles it. -- fixed in 8a5135105 and, in the damage chain, da49bad71: the canonical x2 (Paolo, 2026-09-23)
- **medium** *Neutralizing Gas, Quick Draw, Supersweet Syrup: behaviour the reference doesn't...* -- In konefr's game these three abilities do nothing (the only reference reads of Neutralizing Gas are in Skill Swap/Role Play blocklists). Here Neutralizing Gas suppresses every other ability on the field, Quick Draw goes first 30% of the... -- fixed in 5cc4657ac, 9065e8fd1, 69e9b0307, 7d65064ea (the games' list and the Ability Shield) and 32f889c10 (Cud Chew)
- **medium** *Ice Scales stacking* -- konefr's game quarters special damage against Ice Scales in a single battle (two iterations) and cuts it to 1/16 in a double. The port halves it once. That breaks the port's own rule, which keeps the reference's Tablets/Vessel of Ruin and... -- fixed in 96aecbd40, with the Ruin and Fairy Aura copies in 2ea835b90 and 6e32d1091
- **medium** *Sand Rush / Sand Force sandstorm immunity* -- Sand Rush and Sand Force holders take sandstorm chip damage here. In the reference they are immune. Both are listed as IMPLEMENTED. -- fixed in a39e0941c
- **medium** *Infiltrator: Safeguard and Mist* -- Here Infiltrator gets past substitutes and screens, but Safeguard still blocks its status moves and Mist still blocks its stat drops. In the reference it gets past all four. -- fixed in 91661b739

### battle-mechanics

- **high** *AI move scoring* -- The trainer AI is still assembly. It reads a move's power and effect at ctx+0x3E1 / ctx+0x3DE + move*16, i.e. trainerAIData.moveData[], which is sized NUM_MOVES+1 = 468 (MOVE_SHADOW_FORCE). For every added move (ids 468..923) the AI reads... -- fixed in cd953fc55, cd60f9ab5, 7a7d4d35b, f083fc484 (matching) and 71d621a8d
- **high** *priority* -- The importer parses priority with `\d+` and drops the sign. Circle Throw and Dragon Tail have priority +6 here against the reference's -6, and Beak Blast and Shell Trap +3 against -3. So these phazing and charge moves act first instead of... -- fixed in 93831a0b8
- **high** *critical hits* -- Crit damage multiplier: ours is Gen 4 (x2, Sniper x3) as an integer multiply of the whole damage. The reference is Gen 6+ (x1.5, with Sniper an extra x1.5 final modifier, i.e. x2.25) and applies it after spread/weather, before the random... -- fixed in 2d23101cb
- **high** *critical hits* -- Crit-stage odds differ. Ours is Gen 4: 1/16, 1/8, 1/4, 1/3, 1/2. The reference is Gen 7+: 1/24, 1/8, 1/2, always, always. At stage 0 ours crits more often; at stages 2 and above the reference crits far more often, and is certain from stage... -- fixed in 988ff1cca
- **high** *speed* -- Paralysis quarters Speed in ours (Gen 4). The reference halves it (Gen 7+). -- fixed in 52b868456
- **high** *status* -- Burn residual damage is 1/8 max HP in ours (1/16 with Heatproof). The reference uses 1/16 (1/32 with Heatproof). -- fixed in a303f21b0
- **high** *damage formula* -- Ours halves the target's Defense for MOVE_EFFECT_HALVE_DEFENSE (Explosion, Self-Destruct), which is Gen 4. The reference's Gen 5+ calc has no such halving even though the effect id is kept, so these moves hit about twice as hard here. -- fixed in e9c2a1994
- **medium** *damage formula* -- The damage pipeline is Gen 4 integer arithmetic in ours and a Gen 5+ Q4.12 chain in the reference, with a different order. Ours: base damage with burn, screens, spread and weather folded in before +2, then crit, then Life Orb/Metronome/Me... -- fixed in da49bad71, 750ed6e3f, 03c9be267, a1977c9b9 and 8b00ee2a2; the two pieces left are rows of their own

### config-toggles

- **high** *APPLY_ANTIPIRACY* -- The reference makes the overlay-1 card check return TRUE and neuters the DSProt checks in overlay 123, so the game runs on flashcarts (R4, TWLmenu). Our tree keeps HeartGold's anti-piracy whole: the card-status test, the... -- fixed in 0ba297111
- **high** *Capture formula...* -- The ball-generation toggles sit inside a Gen-8/9 capture formula that we did not port. Our tree keeps HeartGold's Gen-4 formula with only the per-ball multipliers changed, so catch odds differ nearly everywhere. The reference has a... -- fixed in 540cd1805
- **low** *IMPLEMENT_CRITICAL_CAPTURE (critical roll outcome)* -- When the critical roll hits in the reference, only one shake check runs and the result is returned without the critical mask, so the throw fails after one shake. Ours deliberately treats the roll as animation only. (already in the ledger) New evidence from the third round: in the reference the critical roll never succeeds, because criticalCatchRate multiplies an int by a u32 and wraps; so the fix Paolo chose (2026-09-23) is the intended one-check capture rather than either of the reference's outcomes. -- fixed in 82eef4cd7
- **low** *FRIEND_BALL_GENERATION* -- In the reference, a Pokémon caught in a Friend Ball ends with 200 friendship: the second, unconditional block overwrites the generation-9 value of 150 on every successful catch. Ours sets 150. -- fixed in 5916a9f43: the reference's 200 is its own slip against its config, and 150 stays
- **medium** *EXPERIENCE_FORMULA_GEN* -- The reference's scaled formula uses the truncated integer square root and divides by the number of participants after scaling. Ours uses a fixed-point root and scales after dividing, so every award where the two levels differ is a... -- fixed in 7446d9129
- **medium** *HIDDEN_ABILITIES / HIDDEN_ABILITIES_FLAG / HIDDEN_ABILITIES_STARTERS_FLAG* -- In the reference, a script sets flag 2600 and the next generated Pokémon gets its hidden ability, stored as a persistent bit that survives ability resets and evolution; flag 2601 does the same for starters. None of this exists here: only... -- fixed in e1ceb7fc2
- **high** *MEGA_EVOLUTIONS / PRIMAL_REVERSION / FLAG_MEGA_EVOLUTION_ENABLED* -- Mega Evolution (enabled by flag 2518) and Primal Reversion are enabled behaviour in the reference and are not implemented here. The ledger defers them because the data is missing, but the mega stones, the orbs, Kyogre, Groudon and every... (already in the ledger) -- out of scope: SCOPE.md, until asked for
- **medium** *IMPLEMENT_SEASONS* -- In the reference, Deerling and Sawsbuck in the party take the current season's form, (month-1)%4, on the RTC update. That is not implemented here, and because our forms are separate species it would mean switching species. (already in the ledger) -- out of scope: SCOPE.md, until asked for

### decompiled-not-patched

- **medium** *asm edited instead of decompiled: thirty boxes* -- The thirty-box behaviour goes through 15 assembly sites in four overlays that were never decompiled. Commit 8a02692c2 swapped literal #0x12 for #NUM_BOXES while NUM_BOXES was still 18, and 668543b5d set it to 30 seven minutes later. From... -- fixed in a5572a571, 92e6f8635, f2b47a5d4, ba1936a9a, ab23a4e1a, d0cc6dcde, 9e4f16bf3, a15bd2e4e, 63d625d34 (matching), 1d92c1b9e and d48caed89
- **medium** *asm edited instead of decompiled: battle script command table* -- The 69 added opcodes are dispatched through a table the port extended in assembly (commit dc0c05324 and later ones) rather than moving battle_command.c's rodata into C. The opcode names do match the reference one for one, so this is about... -- fixed in 5d57ce707, matching: five objects in retail's order, since mwccarm gives each unread table its own section and sorts a file's sections by size
- **low** *asm edited instead of decompiled: Moon Ball list* -- The Moon Ball species table was rewritten from 14 entries to 6 inside the asm rodata (commit 2731f5f2a), and the C extern was resized by hand to match. The values match the reference, but the table's only definition is assembly, and its... -- fixed in 5d57ce707
- **low** *asm edited instead of decompiled: move relearner's type-icon offset* -- Commit 10e4a7f1d changes behaviour in an undecompiled relearner routine. The contest-condition icon index now starts at 19 instead of 18, done by editing the immediate in assembly. -- fixed in e13a7893d (matching)

### encounters-world

- **high** *daycare / egg species (breeding added species)* -- The table that maps a mother's species to its egg species (pms.narc) is still the cartridge's 508-entry file here. Every species past it (Victini and all 532 added species the reference's BabyMons.c lists, plus the forms) gets egg species... -- fixed in 9611f21bc
- **high** *daycare / egg moves* -- Egg moves are still Gen 4's here. The list covers 198 species up to Weavile (459), so no added species can inherit an egg move. The reference has modern egg-move lists for 900 species, and all 198 species that overlap have a different... -- fixed in 0d9b59745
- **medium** *marts / price display* -- Ours shows mart prices with 4 digits; the reference patched this to 6. The imported prices put HP Up, Protein, Iron, Calcium, Zinc and Carbos at 10000, and they are sold at Goldenrod Dept. Store 4F (_020FBAFA) and Celadon 5F.... -- fixed in 2b2c88c5d (matching) and ca6fa5d9f
- **medium** *marts / specialty mart inventories* -- With MART_EXPANSION enabled (config.h:176), the reference repoints all 30 specialty-mart slots to its own lists. Three disagree with ours: (1) the two Goldenrod Dept. Store 2F clerks are swapped: SpecialMartBuy 3 sells Poke Balls, Repels... -- closed (Paolo, 2026-09-23): retail kept; hg-engine's three differences -- the Goldenrod 2F clerks swapped, Heart Mail at Ecruteak, Air Mail at Lavender -- are copies of another list, defects rather than values
- **medium** *field / following Pokemon overworld sprites* -- In the reference every species, the added generations and forms included, has its own overworld sprite that walks behind the player. Here any species past Arceus, and every form species from 1042 up, resolves to offset 0 and walks as a... -- fixed in c9b25ef19, ff45dc694, e264195e0, 39fb439f8 (matching), ab37eaf60, ee3e2d598 and ab37297a1; seen walking on Route 29 in the diagnostics build
- **medium** *gift / wild / egg / starter Pokemon: hidden-ability script flags* -- The reference lets a script set flag 2600 so that the next Pokemon made gets its hidden ability: a wild or scripted static encounter, a givemon gift, a give_egg egg or the Togepi egg. Flag 2601 does the same for the starter, and an egg's... -- fixed in e1ceb7fc2
- **medium** *daycare / Mirror Herb and same-species egg-move sharing* -- When the second Pokemon is deposited, the reference copies egg moves between the two parents into their empty move slots if either holds a Mirror Herb or both are the same species (a Gen 9 mechanic). Ours only strips the Griseous Orb and... -- fixed in feae85dac
- **low** *field / seasons (Deerling, Sawsbuck)* -- In the reference, on the same real-time-clock tick that returns Shaymin to Land Forme, every Deerling and Sawsbuck in the party takes the form for the current month (month-1 mod 4). Ours has the species and their four season forms as... (already in the ledger) -- out of scope: SCOPE.md; the species and forms are here

### items

- **critical** *item data loading (every GetItemAttr on an imported item)* -- Commit d02b649f1 reduced sItemNarcIds to 564 rows and sent ids 564 and up through GetItemIndexMapping. LoadItemDataOrGfx was not updated and still indexes the short table with the raw item id, so for every imported item (564..2692) it... -- fixed in c708be76a
- **high** *item price (20-bit) record layout* -- The price nibble is written at one offset and read at another. The data puts it at 0x20; the reader takes byte 0x22. That byte is outside the 34-byte record: it is the archive's 0xFF alignment padding in LoadAllItemData's bulk copy, and... -- fixed in c708be76a
- **high** *TM/TR machines past HM08* -- The data gives 340 items fieldUseFunc 6 (the TM routine), in both trees. Here only the 100 retail machines map to a move. The other 240, including TM093 (Flash Cannon), TM100 (Confide) and TR00..TR99, reach field_use_item.c:364... -- fixed in 3396fa25c, e6ebb535d (konefr's), 6a03ab16e, 8f2a6d88c, 048fee769 and 8c902ecf2
- **high** *hold effects with a reference consumer and none here* -- About 27 imported items carry a hold effect that does something in the reference and nothing here. A Gem is never used up and gives no ×1.3. Red Card, Eject Button and Eject Pack never force a switch. The origin Crystals, Blank Plate and... (already in the ledger) -- fixed in 6319d8b66, 71957fc85, 555cf7ddd, c547ebf36, 561bf97a2, c8d1df490, 85e179f80, 062d94fda, 028afa5bf, de4ae9177, 5f7901f49, 7c4954758 and bf6020910: every one of the 64 is read; where they fall short of the games is a row of its own
- **medium** *Pixie Plate / Arceus Fairy* -- An Arceus holding a Pixie Plate stays Normal here: its form, its battle type and Judgment's type. In the reference all three become Fairy. Only the ×1.2 Fairy move boost was ported (overlay_12_0224E4FC.c:7513). -- fixed in 0dfce6d96
- **medium** *Ability Patch (party use)* -- In the reference the Ability Patch switches a Pokemon to or from its hidden ability. Here its record (fieldUseFunc 1, partyUse 1, no party-use bits set) falls through to CanUseItemOnMonInParty, which finds nothing to do, so it has no... -- fixed in 41a032808
- **medium** *field-use routines 30/31/34/35* -- Seven items (four Nectars, Reveal Glass, DNA Splicers, Rotom Catalog) sit in the bag and do nothing here. In the reference they open the party menu and change form. The 21 Mints and the Ability Capsule now have routine 1 and their own... (already in the ledger) Routines 30, 34 and 35 in 6aaa274f0 and 359d9af83 (Enamorus added to the Reveal Glass, the catalog's egg and leaked Strings not copied). DNA Splicers is left: it keeps the fused Reshiram or Zekrom in the save, in hg-engine's SAVE_MISC_DATA extension, which konefr's include/config.h enables (ALLOW_SAVE_CHANGES at ccf2c9f5), so the port does it the same way. -- fixed in 34eb81136, ab8abb720 and 36a24a778; with 30, 34 and 35 of round 5; fuse, save, reload and separate seen in the harness, and a save from before the change still loads
- **medium** *Soul Dew* -- Soul Dew uses the Gen 4 rule here: ×1.5 Sp. Atk and ×1.5 Sp. Def, except in the Frontier. In the reference it uses the Gen 7+ rule: ×1.2 power on Dragon and Psychic moves only, and no defence boost. -- fixed in 48461909f

### level-cap-exp

- **high** *experience formula (scaled, EXPERIENCE_FORMULA_GEN = GEN_LATEST = 9)* -- The reference truncates sqrt(2L+10) and sqrt(L+Lp+10) to whole numbers; ours computes them almost exactly. Almost every experience award therefore differs from the reference's. -- fixed in 7446d9129
- **low** *experience split order and minimum of 1* -- Ours divides and applies the minimum of 1 before scaling, then scales the combined participant and Exp. Share amount once. The reference scales first and then divides each part with its own minimum of 1. The results can differ by rounding... -- fixed in 7446d9129
- **medium** *effort values at the cap once the cap is 100* -- After Morty the reference's cap is 100, so a level-100 Pokémon still gains effort values from battles. Ours excludes cap == MAX_LEVEL, so a level-100 Pokémon gains none, which is vanilla behaviour. -- fixed in 5aa2a726d
- **low** *effort values for a fainted Pokémon at the cap* -- A capped Pokémon with 0 HP that holds an Exp. Share, or still has its participation bit (for example it went down in the same turn as the foe), gets effort values in the reference but not in ours. -- fixed in 5aa2a726d
- **medium** *per-stat effort-value cap in battle (and Frontier Pokémon)* -- Battle effort-value gains stop at 252 per stat in the reference and at 255 in ours. The same byte patch lowers the Frontier Pokémon clamp to 252, which ours keeps at 255. -- fixed in ef7e24025
- **low** *Rare Candy repeat use* -- In the reference, when a Rare Candy does not trigger an evolution and more are in the bag, the party menu stays open on 'Use on which Pokémon?' so the next candy can be used straight away. Ours always goes back to the bag. -- fixed in cb4d522f2
- **low** *Rare Candy at level 100 (evolution): friendship* -- Using a Rare Candy on a level-100 Pokémon that can evolve raises friendship (+5/+3/+2) in ours but not in the reference. Everything else about it matches: the candy is accepted, consumed, and the evolution is found in case 6. -- fixed in 050fe76b7

### move-effects

- **high** *move data: priority* -- Every added move with negative priority is written with the sign dropped. Circle Throw and Dragon Tail are +6 here (the highest priority in the game) instead of -6. Beak Blast and Shell Trap are +3 instead of -3. -- fixed in 93831a0b8
- **high** *move data: effect (Triple Dive)* -- Triple Dive was given Triple Kick's effect. Past effect 276 the importer matches effects by name, and pret's name for Triple Kick's effect 104 is the same as the reference's name for its flat three-hit effect 299. So Triple Dive checks... -- fixed in 93831a0b8
- **high** *move effect: Solar Seeds (konefr's own move)* -- Solar Seeds always hits exactly twice. In the reference it hits 2-5 times, with Skill Link and Loaded Dice applying. -- fixed in 605d7b8d2
- **medium** *move effect: 2-5 hit distribution* -- Multi-hit moves use Gen 4 odds here (2 and 3 hits 37.5% each, 4 and 5 hits 12.5% each) instead of the reference's Gen 5+ odds (35/35/15/15). This covers Bullet Seed, Pin Missile, Rock Blast, Scale Shot and the rest. -- fixed in 337d90c70
- **medium** *move effect: Triple Axel* -- Triple Axel hits 20/20/20 here instead of 20/40/60. -- fixed in 40ea686b0
- **high** *move data: FLAG_UNUSED_MOVE (bit 5) not enforced* -- The reference marks 79 moves as unusable. They are skipped when learnt by level, removed from trainer parties, and cannot be selected in battle. Ours reuses the bit for King's Rock and has no such gate, so all 79 can be learnt and used. In... -- fixed in 93f7e9783 and 5f8ccce35
- **medium** *King's Rock / Stench flinch eligibility* -- King's Rock works on different moves here. By the reference's rule, 152 moves can flinch with it that cannot here (85 of them retail: Thunderbolt, Flamethrower, Ice Beam, Psychic, Body Slam, Fire Punch...). 53 can flinch with it here that... -- fixed in 80126888f
- **high** *Protect variants: contact penalties and status moves getting through* -- All six moves exist in this game, but here they are plain Protect. There is no penalty for making contact, and the four that should let status moves through block them too. -- fixed in 92a013063 and ef6fc497d

### species-forms

- **high** *form resolution (PokeFormDataTbl / GetSpeciesBasedOnForm)* -- Ours has no run-time mapping from (base species, form number) to the form's record. In the reference every lookup goes through PokeOtherFormMonsNoGet/GetSpeciesBasedOnForm: personal data, experience, hidden ability, TM, tutor and level-up... -- fixed in 9eca47d8b
- **high** *TM/HM compatibility of forms and level-up TM moves* -- The reference gives a form with no MachineMoves of its own its base species' TM list, and makes every TM whose move the species learns by level-up compatible. Our importer does neither. As a result 324 of the 396 forms here have tms [] and... -- fixed in 419489cfe
- **high** *Mega Evolution and Primal Reversion* -- Both toggles are enabled in the reference, and neither mechanic exists here. The ledger defers them on grounds that are no longer true. The 98 mega stones are items here, Kyogre and Groudon are species (and always were in HGSS), their... (already in the ledger) -- out of scope: SCOPE.md; the data and species are here, New Gold never turns Mega on
- **high** *in-battle form changes (ability- and move-driven)* -- None of the reference's post-Gen-4 battle form changes exist here. The form species' records (e.g. DARMANITAN_ZEN_MODE, AEGISLASH_BLADE, WISHIWASHI_SCHOOL, MIMIKYU_BUSTED, MINIOR_CORE_*) are present but nothing ever switches a battler to... (already in the ledger) -- fixed in dccdffd61, 093a088c6, 190f47df0, 43c7939be, 51dfc038b, 951b24c75, 1e10eb8a1, 061d80c02, d815aa3fb, 285871719 and f9bea4a3b
- **medium** *passive wild forms (female forms, 1% forms)* -- In the reference, half of all wild Unfezant, Frillish and Jellicent are generated in their female form (which is 100% female), and 1 in 100 wild Dunsparce and Tandemaus carry form 1 into Dudunsparce Three-Segment and Maushold Family of... -- fixed in a2b8840d9
- **medium** *battle-start forms and form reversion* -- The reference forces Xerneas Active and crowns Zacian or Zamazenta holding the Rusted Sword or Shield (with the move swap) at the start of every battle. At switch-out and battle end it reverts every NEEDS_REVERSION form to the form listed... (already in the ledger) -- fixed in 70ae8b468 and d815aa3fb
- **low** *reversion side effect: bad poison at battle end* -- The reference's reversion routine also turns badly poisoned into regularly poisoned for the whole party at battle end, before any form check. Here a badly poisoned Pokemon stays badly poisoned into the next battle. -- fixed in ce7ab602c
- **medium** *hidden-ability grant at run time (HiddenAbilityTable use)* -- In the reference, a script sets flag 2600 before a wild battle, a gift or an egg, or flag 2601 before the starter, and that Pokemon gets its hidden ability. A persistent bit then re-applies the (form-adjusted) hidden ability after... -- wrong: the mechanism is real but no content uses it -- the flags are defined and nothing sets them, at either revision

### tests-honesty

- **high** *battle / protect-variant moves* -- Wide Guard, Quick Guard, Crafty Shield and Mat Block (move effect 373) set no protect flag at all, so they protect nothing. King's Shield, Spiky Shield, Baneful Bunker, Obstruct, Silk Trap and Burning Bulwark (effect 111) are plain... -- fixed in 92a013063 and ef6fc497d
- **high** *tests / move-effect and battle-command ratchets* -- The STILL_TO_DO=0 ratchet counts script shapes, not behaviour. Once an effect script contains anything besides a bare hit, removing all of its C still leaves the test green. The STUBS ratchet only checks that no script here runs a stub, so... -- fixed in 6a69b0cd2
- **medium** *battle / Hurricane and the three Storms accuracy* -- Hurricane (effect 345), Bleakwind Storm (360), Wildbolt Storm (361) and Sandsear Storm (362) keep their normal accuracy in rain, where the reference never misses. Hurricane also keeps it in sun, where the reference drops it to 50. -- fixed in a8aaf4ea1
- **medium** *tests / what counts as an implemented ability* -- Schooling and Power Construct are counted as having an effect only because their names appear in the Skill Swap, Role Play, Worry Seed, Gastro Acid and Simple Beam ban-lists. The form changes konefr implements for them in C are absent... -- fixed in 2bd391076
- **medium** *ratchets / species-in-play and pending abilities after the forms import* -- The species ratchet was raised from 18 to 58 in the forms commit with no reason written down, while its own comment says it may only come down. The comment above SPECIES_IN_PLAY and the docstring still describe the 1027 range. The ledger's... (already in the ledger) -- fixed in 3bd3b3ce9
- **medium** *items / form-change items pinned inert* -- The test pins konefr's four form-change item routines as doing nothing, citing missing forms. After today's import those forms exist: SPECIES_TORNADUS_THERIAN 1171, KYUREM_WHITE/BLACK 1174/1175 and ORICORIO_POM_POM 1239 (the Rotom forms... (already in the ledger) -- fixed in d2f08a629
- **high** *move tutor / species index (committed today, 16955ee61)* -- Every species from 508 on reads the tutor record two places later. Lillipup (508) gets Stoutland's record 507, and so on. Species 1436 and 1437 seek to byte 11480 and 11488 of an 11480-byte file, so FS_ReadFile reads nothing and the... -- fixed in 9eca47d8b
- **medium** *battle / RESTORE_ITEMS_AT_BATTLE_END* -- We differ from konefr in three ways. (1) A Kee, Maranga or Roseli Berry eaten in battle comes back afterwards, because the berry range stops at Rowap. Kee and Maranga are eaten on a hit here (overlay_12_0224E4FC.c:6500-6507), so they... -- fixed in 7c0dd508e

### trainers-gyms

- **high** *trainer AI / move data* -- konefr's AI scores every move from the full move table. Ours reads `ctx + 0x3DE + move*16` with no bound, so for any move numbered above 467 it takes power, type and effect from whatever BattleContext fields sit after moveData. The... -- fixed in cd953fc55, cd60f9ab5, 7a7d4d35b, f083fc484 (matching) and 71d621a8d
- **high** *trainer Pokemon personality: ability slot translation* -- The PID modifier carries over from one party member to the next in both engines, but the translated bytes do different things to it. (1) konefr's HIDDEN slot 0x02 also forces the personality to genderRatio-2, and that stays in force for... -- fixed in 0d67a2a91 and 8143a8117
- **high** *platform capacity: species on a trainer* -- No species numbered 1024 or above can be a trainer Pokémon here: 18 species (Iron Valiant through Pecharunt) and all 396 form species. The mask wraps it, so SPECIES_KORAIDON (1025) is read as species 1, form 1. konefr's engine takes any... -- fixed in 166d1b1c0
- **medium** *trainer Pokemon personality: species numbers* -- 30 species konefr's trainers use have different numbers here, so their personality seed and nature differ from his build. In scope: Morty's Annihilape (Bashful→Jolly), Whitney's Farigiraf (Calm→Adamant), Carrie's Herdier, Al's Joltik,... -- fixed in fd3f21ba5
- **medium** *trainer moves: BLOCK_LEARNING_UNIMPLEMENTED_MOVES* -- In konefr's build, Morty's Annihilape (Trainers.c [31]) enters with Bulk Up / (empty) / Drain Punch / Taunt, and Issac's Whismur [391] loses Echoed Voice. Ours gives both the move. Trainers.c holds 79 moves flagged this way, so any trainer... -- fixed in f37822a03 and 5f8ccce35
- **medium** *trainer names: length limit* -- {TRNAME} names stay packed at 9 bits per character plus a marker. 'Pippo Franco' packs to 9 units and 'Pietro Pacciani' to 10, so both fit konefr's 12 but not our 8. In a clean build, CopyStringToU16Array asserts and copies nothing, so the... -- fixed in 80fc2f0a1; not yet seen on screen
- **medium** *trainer names: stale build of the name bank* -- The working-tree build names Route trainers 47 and 383 Mikey and Peter, where konefr's are Pippo Franco and Pietro Pacciani. It also still has the vanilla '{TRNAME} -' (0) and bare 'Cheryl'…'Darach' (667-671, 707-711). Editing... -- fixed in 2348f092f
- **medium** *trainer dialogue* -- konefr's dialogue for three trainers is missing. In battle, ours' Proton (Slowpoke Well) says vanilla LAST_POKE and LAST_POKE_HALF lines konefr removed, and a different defeat line. Pippo Franco and Pietro Pacciani speak vanilla Mikey and... -- fixed in 56b3cfacf

